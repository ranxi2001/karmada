The leaked taint fixture contributes `30m` to the only cluster this consumer can use: member1. The fixture is a [3-replica Deployment requesting `10m` CPU per replica](https://github.com/karmada-io/karmada/blob/3d4d14d746de507164abf40c1017b1f2b0e47e3a/test/helper/resource.go#L190-L218). Assumptions created for member2/member3 by the same reschedule are irrelevant to this test.

The Flink test [targets member1, adds `200m` per FlinkDeployment, and stops at the first no-fit](https://github.com/karmada-io/karmada/blob/3d4d14d746de507164abf40c1017b1f2b0e47e3a/test/e2e/suites/base/estimator_test.go#L379-L515). The foreign `30m` does not need to exhaust member1 by itself; it only needs to push one `200m` attempt over the capacity boundary. In the [official job](https://github.com/karmada-io/karmada/actions/runs/28998390044/job/86054168911), the member1 estimator request at `07:15:22.997` contained five assumptions, including `deploy-wbch9` with 3 replicas at `10m`. The member1 probe request at `07:15:24.665` contained four assumptions and no `deploy-wbch9`; the `200m` probe then fit. This member1-only delta is the relevant contamination.
