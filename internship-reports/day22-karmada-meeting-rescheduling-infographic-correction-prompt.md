# Day 22 Meeting Rescheduling Infographic Correction Prompt

Edit the supplied infographic while preserving its exact 16:9 composition, five-stage layout, colors, icons, arrows, spacing, title, subtitle, bottom evidence boundary, and impact meters.

Make only these text corrections:

1. In stage 1 supporting text, remove the visible trailing vertical-bar characters. The exact lines must read:
   "Set target watermarks"
   "Rank workloads by balancing benefit"
   "Stop when pools recover"

2. In stage 2 supporting text, remove the visible vertical-bar character. The exact lines must read:
   "WorkloadRebalancer is the user-facing rescheduling command"
   "Automation may trigger it"

3. In the stage 2 CR/API document, replace the incorrect line `policy: reschedule` with the exact line:
   `strategy: SafeMigration`
   Keep the other example lines `resource: workload-x`, `from: cluster-a`, `to: cluster-b`, and `unit: Pod | data shard` unchanged.

4. In stage 3 supporting text, replace the current wording with these exact lines and no vertical bars:
   "Delete source and create target in parallel"
   "Deletion finishes faster"
   "Service is impaired"

5. In stage 5 supporting text, remove the visible vertical-bar character. The exact lines must read:
   "CPU/GPU: Pod units"
   "Sharded workloads: data-shard units"

Do not alter any other visible text. Keep all text English-only, sharp, correctly spelled, and readable. Do not add new content, logos, decorations, gradients, or watermarks.
