请只重写参考图第二栏下方的黄色说明框，彻底删除其中错误的英文对象名 `GracefulExecutionTasks`。修正后的完整说明必须是：

「现有优雅驱逐任务能暂留整个 Source Work，
但没有 unit identity / stableWindow」

黄色框里不要再出现 `GracefulExecutionTasks` 或任何以 `Graceful` 开头的连写英文词。必须保持参考图的 16:9 尺寸、白色背景、三栏布局、配色、图标、箭头以及其他全部文字不变。不要改写、翻译、删减或新增其他内容。尤其不要改变 `PreserveReady`、`Binding.spec.clusters`、`dynamicScaleDown`、`finalizer`、`deletionTimestamp`、`direct deletion` 等技术词。
