可以，按这个顺序做。**这次先不要继续改功能代码，先把机制实验做明白。**

# #7830 行动方案

### 1. 先 rebase，清掉历史包袱

#7837 已经在 **2026-08-17** merge 到 master，commit 是 `1dd55a5`；而 #7830 页面目前仍显示 2 个 commits，并且之前还是 stack 在 #7837 上的。先让 diff 只剩你真正要做的东西。([GitHub][1])

```bash
git fetch upstream

git checkout feature/multi-component-scale-rescheduling

git rebase upstream/master

git push --force-with-lease origin feature/multi-component-scale-rescheduling
```

rebase 后第一件事：

```bash
git diff upstream/master...HEAD --stat
git diff upstream/master...HEAD
```

**先别修冲突之外的任何代码。**

---

### 2. 做 mentor 要的核心实验：v1alpha1 请求到底怎么进入 webhook

这个实验是当前最高优先级。

临时在 ResourceBinding validating webhook 入口打印：

```go
klog.Infof("kind=%+v", req.Kind)
klog.Infof("requestKind=%+v", req.RequestKind)
klog.Infof("resource=%+v", req.Resource)
klog.Infof("requestResource=%+v", req.RequestResource)
klog.Infof("subResource=%q", req.SubResource)
klog.Infof("requestSubResource=%q", req.RequestSubResource)
klog.Infof("object=%s", string(req.Object.Raw))
klog.Infof("oldObject=%s", string(req.OldObject.Raw))
```

**不要提交这段代码，只用于实验。**

Kubernetes 官方定义是：当 webhook 只注册 v1alpha2、`matchPolicy: Equivalent`，客户端请求 equivalent 的旧版本时，API Server 可以转换后再调用 webhook；此时 `kind` 是 webhook 实际收到的转换后版本，而 `requestKind` 保留客户端原始版本。([Kubernetes][2])

但这次你不要靠文档直接下结论，**实际跑 Karmada。**

---

### 3. 跑 4 组请求，把结果记录成表

先用 **v1alpha2** 创建一个确实有 component scheduling result 的 Binding：

```yaml
spec:
  components:
  - name: worker
    replicas: 3

  clusters:
  - name: member1
    replicas: 3
    components:
    - name: worker
      replicas: 3
```

然后跑：

```text
Case A
v1alpha2 GET / UPDATE main resource

Case B
v1alpha1 GET / UPDATE main resource

Case C
v1alpha2 UPDATE /status

Case D
v1alpha1 UPDATE /status
```

最终你要得到这样一张表：

| Case        | webhook 是否触发 | Kind | RequestKind | Object 有 components? | OldObject 有 components? | 最终存储 components 是否还在 |
| ----------- | ------------ | ---- | ----------- | -------------------- | ----------------------- | -------------------- |
| v1a2 main   | ?            | ?    | ?           | ?                    | ?                       | ?                    |
| v1a1 main   | ?            | ?    | ?           | ?                    | ?                       | ?                    |
| v1a2 status | ?            | ?    | ?           | ?                    | ?                       | ?                    |
| v1a1 status | ?            | ?    | ?           | ?                    | ?                       | ?                    |

**这个表比你写 500 行代码有价值。**

---

### 4. 特别验证两个问题

#### 问题 A：`APIReader.Get()` 到底有没有必要？

你当前 PR 明确写着：

> v1alpha1 guard uses an uncached v1alpha2 read

而且为此给 webhook wiring 增加了 `APIReader`。([GitHub][3])

实验重点看：

```go
req.OldObject
```

如果 v1alpha1 UPDATE 经过 conversion 后：

```text
req.Object
    = 新对象，v1alpha2 表示

req.OldObject
    = 旧对象，v1alpha2 表示
    = components 信息完整
```

那么：

```go
APIReader.Get(...)
```

大概率直接删除。

因为 admission 自己已经把 old state 给你了，没理由再访问 apiserver。

---

#### 问题 B：`/status` guard 到底有没有必要？

你目前 PR 专门增加了 v1alpha1 `/status` compatibility handling。PR 描述也明确把 main-resource 和 `/status` 都纳入了 guard。([GitHub][3])

实验不要只看 webhook：

```text
v1alpha1 GET
→ 修改 status
→ PUT/PATCH /status
→ 再 v1alpha2 GET
```

最后直接检查：

```yaml
spec.clusters[].components
```

还在不在。

**如果还在，就把整个“v1alpha1 status 会丢 components”的理论删掉。**

---

### 5. 实验完以后再决定删什么

我建议你的默认态度变成：

> **没有实验或者明确 API contract 支持的代码，一律删。**

优先审查这些：

```text
APIReader
↓
很可能删

v1alpha1 /status webhook
↓
很可能删

专门的 status compatibility E2E
↓
跟着行为一起删

webhook config 为版本兼容新增的规则
↓
重新评估

v1alpha1 write guard
↓
等实验结果再决定

feature-gate old/new 大量 comparison
↓
重新问：PRD 是否真的要求 admission 层保证？
```

尤其你现在 #7830 的 PR 描述已经膨胀到：membership、duplicate、`requiredBy`、feature-gate rollback、v1alpha1 main/status protection。([GitHub][3])

**不要默认这些都应该留下。**

---

### 6. 然后重新读 PRD，只回答三个问题

拿着 PRD，不看现有实现，自己写三行：

```text
① 7830 要保护的具体 invariant 是什么？

② 谁会破坏这个 invariant？

③ 如果不校验，实际哪个 consumer 会出问题？
```

比如：

```text
TargetCluster.components 中不能出现重复 component

谁会破坏？
scheduler / API client？

不校验会怎样？
scheduler 后续读取时语义不确定？
```

如果第三问回答不了：

> “可能有风险”“理论上不安全”

先不要写。

要能回答：

> **哪个函数/模块会读取这份数据，它依赖什么假设，违反假设后具体会怎样。**

---

### 7. 把 `requiredBy`、feature gate 单独重新审一次

这两个特别容易让 scope 爆炸。

对每一个都画 producer/owner/consumer：

```text
requiredBy[].clusters[].components

producer = ?
owner    = ?
consumer = ?
为什么必须由当前 Binding webhook 校验？
```

如果你无法明确回答，就不要因为“类型也是 `[]TargetCluster`”而复用 validation。

这就是这次 `GracefulEvictionTask` 给你的教训：**struct 长得像，不代表协议语义一样。**

---

### 8. 最终把 PR 压到“最小可解释 diff”

你最终应该能面对 mentor 用 30 秒解释：

> #7837 introduced the component scheduling result.
> After rebasing, I verified how v1alpha1 requests are converted before admission.
> I removed X/Y/Z because Kubernetes already guarantees those behaviors.
> #7830 now only contains A and B because these are the two invariants that are not guaranteed elsewhere.

如果最后实验发现：

```text
A、B 也没有明确需求
```

**那就允许结论是：#7830 不需要，关闭 PR。**

这不是失败。比为了保住 PR 硬找理由强很多。

---

## 你今天最应该完成的顺序

```text
1. rebase #7837
        ↓
2. 不改功能代码
        ↓
3. 打 AdmissionRequest 日志
        ↓
4. v1alpha1 / v1alpha2 main-resource 实验
        ↓
5. v1alpha1 / v1alpha2 status 实验
        ↓
6. 记录 Kind / RequestKind / Object / OldObject
        ↓
7. 明确 conversion + admission 链路
        ↓
8. 删除 APIReader / status guard 等被证明冗余的代码
        ↓
9. 回到 PRD，重新确定 7830 最小职责
        ↓
10. 重写测试，只测试最终真正需要的行为
```

**今天先做到第 6 步，不要急着继续“修 #7830”。** Kubernetes CRD 多版本里，只有一个版本标记为 storage version，其他 served version 在读写过程中依赖转换；这正是你现在应该先吃透的底层机制。([Kubernetes][4])

等你把 **四组实验日志**贴给我，我可以直接和你一起逐字段分析 `Kind / RequestKind / Object / OldObject`，然后判断 #7830 哪些代码该删、哪些真该留。

[1]: https://github.com/karmada-io/karmada/pull/7837 "Introduce spec.clusters.components field for holding scheduled components by RainbowMango · Pull Request #7837 · karmada-io/karmada · GitHub"
[2]: https://kubernetes.io/docs/reference/access-authn-authz/extensible-admission-controllers/?utm_source=chatgpt.com "Dynamic Admission Control"
[3]: https://github.com/karmada-io/karmada/pull/7830 "feat: validate component scheduling results in bindings by ranxi2001 · Pull Request #7830 · karmada-io/karmada · GitHub"
[4]: https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definition-versioning/?utm_source=chatgpt.com "Versions in CustomResourceDefinitions"


可以。你先不要管代码，先把 **Kubernetes 多版本 + webhook** 理解成下面 3 张图。

### 1. 最核心：v1alpha1 请求，不等于 webhook 收到 v1alpha1

```mermaid
flowchart LR
    A["用户 / Client<br/>提交 v1alpha1 ResourceBinding"]
    B["Kube API Server"]
    C["版本转换<br/>v1alpha1 → v1alpha2"]
    D["Validating Webhook<br/>#7830"]
    E["写入 Storage Version"]
    F["etcd"]

    A -->|"请求 v1alpha1"| B
    B --> C
    C -->|"webhook 实际看到 v1alpha2 对象"| D
    D -->|"允许"| E
    E --> F
```

你之前最容易误解的是这里：

```text
客户端发 v1alpha1
≠
webhook 收到 v1alpha1
```

因为你的 webhook 注册的是 `v1alpha2 + matchPolicy: Equivalent`，API Server 可以先做 conversion，再把 **v1alpha2 对象**送给 webhook。

所以 mentor 让你做实验，就是验证这条链。

---

### 2. `Kind` 和 `RequestKind` 为什么会不一样

假设用户：

```text
PUT /apis/work.karmada.io/v1alpha1/resourcebindings/foo
```

实际可能是：

```mermaid
flowchart TD
    A["Client<br/>请求版本：v1alpha1"]
    B["API Server"]
    C["Conversion"]
    D["AdmissionRequest"]
    E["Webhook Handler"]

    A --> B
    B --> C
    C --> D
    D --> E

    D --- F["RequestKind = v1alpha1<br/>表示：用户最开始请求什么版本"]
    D --- G["Kind = v1alpha2<br/>表示：Webhook 实际收到什么版本"]
```

所以你实验时重点打印：

```go
req.Kind
req.RequestKind
```

你大概率会看到：

```text
RequestKind = v1alpha1
Kind        = v1alpha2
```

这句话你一定要记住：

> **RequestKind 是“从哪来”，Kind 是“现在是什么”。**

---

### 3. 为什么 `components` 会成为问题

两个版本能力不一样：

```mermaid
flowchart LR
    A["v1alpha1 TargetCluster<br/><br/>name<br/>replicas"]
    B["v1alpha2 TargetCluster<br/><br/>name<br/>replicas<br/>components"]

    A -->|"转换到 v1alpha2"| C["name ✅<br/>replicas ✅<br/>components = empty"]
    B -->|"转换到 v1alpha1"| D["name ✅<br/>replicas ✅<br/>components ❌ 无法表达"]
```

比如 v1alpha2 有：

```yaml
clusters:
- name: member1
  replicas: 3
  components:
  - name: worker
    replicas: 3
```

转成 v1alpha1：

```yaml
clusters:
- name: member1
  replicas: 3
```

`components` 不是被“故意删除”，而是：

> **v1alpha1 这个 API 数据结构根本没有地方装它。**

这就是 conversion 机制。

---

## 那你现在 #7830 最大的问题在哪里？

你原来的思路接近这样：

```mermaid
flowchart TD
    A["发现客户端请求来自 v1alpha1"]
    B["我担心 components 丢失"]
    C["Webhook 额外 APIReader.Get()"]
    D["重新读取当前 v1alpha2 Binding"]
    E["检查旧 components"]
    F["决定拒不拒绝"]

    A --> B
    B --> C
    C --> D
    D --> E
    E --> F
```

mentor 的疑问是：

> **你为什么需要自己再 GET 一次？Kubernetes AdmissionRequest 本身是不是已经给了你需要的信息？**

因为 UPDATE webhook 本身有：

```mermaid
flowchart LR
    A["AdmissionRequest"]

    A --> B["Object<br/>准备写入的新对象"]
    A --> C["OldObject<br/>修改前的旧对象"]
```

所以正确实验应该是：

```mermaid
flowchart TD
    A["已有 v1alpha2 Binding<br/>components = worker:3"]
    B["Client 用 v1alpha1 UPDATE"]
    C["API Server conversion"]
    D["Webhook"]
    E["打印 Object"]
    F["打印 OldObject"]

    A --> B
    B --> C
    C --> D
    D --> E
    D --> F

    F --> G{"OldObject 里面<br/>还有 components 吗？"}

    G -->|"有"| H["APIReader.Get 很可能是多余的"]
    G -->|"没有"| I["再研究是否真的需要额外读取"]
```

**这就是你下一步实验最重要的问题。**

---

## `/status` 又是什么问题？

你之前担心：

```text
v1alpha1 更新 status
↓
因为它看不到 components
↓
把 spec.clusters[].components 干掉
```

但 `/status` 的设计本身就是：

```mermaid
flowchart LR
    A["Client<br/>UPDATE /status"]
    B["API Server"]
    C["只更新 status"]
    D["原来的 spec 保留"]

    A --> B
    B --> C
    C --> D
```

也就是说，如果 Kubernetes 已经保证：

```text
/status 不能覆盖 spec
```

那你额外写：

```text
v1alpha1 /status component protection
```

就很可能属于：

> **Kubernetes 已经替你保证了，你又重复实现了一遍。**

所以 mentor 才会觉得 PR 越来越奇怪。

---

# 最终你脑子里只留这一张

```mermaid
flowchart TD
    A["Client"]

    A -->|"v1alpha1 请求"| B["Kube API Server"]
    A -->|"v1alpha2 请求"| B

    B --> C["API Version Conversion"]

    C --> D["Validating Webhook"]

    D --> E{"允许？"}

    E -->|"Yes"| F["转换成 Storage Version"]
    F --> G["etcd"]

    E -->|"No"| H["Reject"]

    D --- I["Kind<br/>Webhook 实际收到的版本"]
    D --- J["RequestKind<br/>Client 原始请求版本"]
    D --- K["Object<br/>新对象"]
    D --- L["OldObject<br/>旧对象"]
```

你现在先把 4 个东西搞懂：

```text
Kind        = webhook 现在看到什么版本
RequestKind = 用户原来请求什么版本

Object      = 用户想写成什么
OldObject   = 原来是什么
```

然后你的实验本质上就只问两个问题：

```text
① v1alpha1 请求进来以后：
   Kind / RequestKind 到底是什么？

② UPDATE 时：
   OldObject 里面有没有原来的 components？
```

如果 **②答案是“有”**，那你现在为了找旧 components 加进去的 `APIReader.Get()` 基本就失去存在理由了。

这就是你今天应该先搞清楚的主线，其他 `requiredBy`、feature gate、E2E 暂时都可以先放一边。

---

## 实验结果：v1alpha1 请求进入 webhook 后发生了什么

### 先说人话

实验回答了 mentor 提出的两个问题：

1. v1alpha1 主资源请求通过 `matchPolicy: Equivalent` 进入 webhook 后，`Kind` 的确是 v1alpha2，
   `RequestKind` 是 v1alpha1。
2. 但是 webhook 收到的 `Object` 和 `OldObject` 都没有原来的 component data。只有 uncached
   v1alpha2 读取仍能看到 storage 中的 component data。

另外做了一个允许写入的反事实：保持相同 webhook rules 和 AdmissionReview 观测，只把 handler 临时改为
`Allowed`。v1alpha1 main 和 `/status` 更新都返回 200，随后用 v1alpha2 GET 时，两处 component 字段都已经
消失。因此，当前 guard 阻止的是本实验中已复现的数据丢失。

### 实验环境

- 时间：2026-08-17
- PR candidate：`e1495093fcc04f6b220699eae79b08423bd0307f`
- Base：`08f8a2016f20fc68544eb7cf66f360620db859b0`
- API Server：Kubernetes `v1.34.0`，临时单节点 Kind
- CRD：`resourcebindings.work.karmada.io`
  - `v1alpha1`: `served=true`, `storage=false`
  - `v1alpha2`: `served=true`, `storage=true`
  - `conversion.strategy=None`
- Webhook：直接运行 candidate 的 `pkg/webhook/resourcebinding.ValidatingAdmission`
- Rules 与 #7830 一致：
  - main resource：`v1alpha2 + matchPolicy: Equivalent`
  - legacy status：`v1alpha1 + matchPolicy: Exact`
- 请求方式：localhost-only `kubectl proxy` 加 raw HTTP PUT；URL 和请求体 `apiVersion` 均显式指定版本，
  没有使用 typed client 或 hub conversion。

每个 Case 使用独立 ResourceBinding。初始 v1alpha2 对象都包含：

```yaml
spec:
  components:
    - name: worker
      replicas: 3
  clusters:
    - name: member1
      components:
        - name: worker
          replicas: 3
```

日志只记录脱敏后的请求元数据、`Object`/`OldObject`/storage read 的长度、component 数量与 SHA-256，
以及响应；不记录完整 Object、annotations、status 或 `UserInfo`。临时证书、kubeconfig 和日志均为 mode
`0600`，未提交到 Git。

### 四组请求结果

表中的 `1/1` 表示 `spec.components` 有 1 项，`spec.clusters[*].components` 合计有 1 项；`0/0` 表示两处
都为空。

| Case | webhook | `Kind` | `RequestKind` | `Object` | `OldObject` | uncached v1alpha2 | HTTP | 最终 v1alpha2 storage |
| --- | --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| v1alpha2 main | 触发 | v1alpha2 | v1alpha2 | 1/1 | 1/1 | 1/1 | 200 | 1/1；annotation 写入成功 |
| v1alpha1 main | 触发 | v1alpha2 | v1alpha1 | 0/0 | 0/0 | 1/1 | 403 | 1/1；annotation 未写入 |
| v1alpha2 `/status` | 未触发 | N/A | N/A | N/A | N/A | N/A | 200 | 1/1；condition 写入成功 |
| v1alpha1 `/status` | 触发 | v1alpha1 | v1alpha1 | 0/0 | 0/0 | 1/1 | 403 | 1/1；condition 未写入 |

两条拒绝响应的 message 均为：

```text
component-aware bindings must be updated through work.karmada.io/v1alpha2
```

v1alpha1 main 的 AdmissionReview 关键字段为：

```text
Kind            = work.karmada.io/v1alpha2/ResourceBinding
RequestKind     = work.karmada.io/v1alpha1/ResourceBinding
Resource        = work.karmada.io/v1alpha2/resourcebindings
RequestResource = work.karmada.io/v1alpha1/resourcebindings
Object          = v1alpha2, components 0/0
OldObject       = v1alpha2, components 0/0
APIReader GET   = v1alpha2, components 1/1
```

这说明 API Server 确实把请求转换成了 webhook 注册的 v1alpha2 GVK，但 conversion 无法重新生成 v1alpha1
中不存在的字段。`OldObject` 也已经是有损结果，不能代替 storage-state read。

v1alpha1 `/status` 的 AdmissionReview 关键字段为：

```text
Kind               = work.karmada.io/v1alpha1/ResourceBinding
RequestKind        = work.karmada.io/v1alpha1/ResourceBinding
SubResource        = status
RequestSubResource = status
Object              = v1alpha1, components 0/0
OldObject           = v1alpha1, components 0/0
APIReader GET       = v1alpha2, components 1/1
```

Exact status rule 不做版本转换，webhook 收到的对象和旧对象都是 v1alpha1 表示。

### 无 guard 反事实

反事实沿用同一 CRD、rules、TLS 和 AdmissionReview 编码，只把 handler 改为记录后返回 `Allowed`。

| 请求 | AdmissionReview 中的 `Object/OldObject` | 写入结果 | 随后 v1alpha2 GET |
| --- | --- | ---: | --- |
| v1alpha1 main | 0/0，storage read 为 1/1 | 200 | component data 变为 0/0；annotation 已写入 |
| v1alpha1 `/status` | 0/0，storage read 为 1/1 | 200 | component data 变为 0/0；condition 已写入 |

该结果与以下源码机制一致：`/status` 请求虽然只允许客户端修改 status，但 status strategy 操作的是该
served-version Store 已经解码出的 v1alpha1 旧对象。这个旧对象无法表达 components；编码回 storage
version 时，丢失的字段没有来源可以恢复。

### Legacy 对象控制组

对两个从未包含 component data 的 ResourceBinding，恢复 candidate 的真实 guard 后再发 v1alpha1 请求：

| 请求 | HTTP | 最终结果 |
| --- | ---: | --- |
| v1alpha1 main | 200 | annotation 写入成功 |
| v1alpha1 `/status` | 200 | condition 写入成功 |

这说明 guard 的判定边界是 storage 中是否已有 component data，不是统一禁止所有 v1alpha1 客户端。

### 当前结论与边界

- `APIReader.Get()` 仍有必要：v1alpha1 main 的 `OldObject` 虽然已经转换成 v1alpha2 GVK，但 component data
  是空的，无法判断 storage 中是否存在新版本字段。
- v1alpha1 `/status` guard 仍有必要：允许同一请求后，v1alpha2 storage 的 component data 确实被清空。
- v1alpha2 `/status` 不匹配当前两条 validating webhook rule，更新成功且 component data 保持不变。
- 当前实验只执行了 namespaced `ResourceBinding`。`ClusterResourceBinding` 走同一 handler 和同构 rules，
  但本轮没有把它写成“已实测”。
- 这是 API Server、CRD conversion 和真实 candidate handler 的组件级实验，不是完整 Karmada 控制面 E2E；
  scheduler、controller 和 member cluster 不在本次验证范围。

实验结束后已停止 webhook/proxy 并删除唯一临时 Kind 集群。probe 源码不会进入 #7830 diff。
