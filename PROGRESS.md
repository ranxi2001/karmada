# PROGRESS.md

这个文件是给 Agent 工作循环用的短记忆，不是日报。每次开始工作先读这里，每次结束只更新关键状态，避免下一轮从零开始。

## Goal

阶段目标：在 2026 年 9 月前拿到 AgentCube Karmada 项目社区席位。

当前主线：围绕 Karmada upstream 真实贡献建立稳定产出能力，不再停留在学习仓库搭建。战略主线切到 #7621 / proposal PR #7662 的复杂工作负载安全重调度，先用源码、会议和状态机证据做高质量 proposal review，再在作者/maintainer 确认后认领独立 test/design slice；#7697 证书轮换继续作为交付维护线直到 merge，不扩大 scope。

## Last Run

- 2026-07-20：用户确认 exact target/text 后，在 upstream issue [#5070](https://github.com/karmada-io/karmada/issues/5070) 发布 [`/assign`](https://github.com/karmada-io/karmada/issues/5070#issuecomment-5022199800)。API 回读确认 comment author/body 正确、issue 仍 open、assignee 已变为 `ranxi2001`。该认领不等于接管仍 assign `@bharathguvvala` 的 umbrella #5172，也不授权直接 revive stale #5425；TODO 已单列责任，下一步先起草并审批 ownership/scope 协调评论，确认 v1.18 Overflow 与 Duplicated failback 的边界后再决定测试或实现。
- 2026-07-20：完成 Day 29 对 [#5070](https://github.com/karmada-io/karmada/issues/5070) 与 proposal PR [#7662](https://github.com/karmada-io/karmada/pull/7662) 的历史、源码和业务语义对齐。current master `e4417e386` 证明 Full/Fresh 只重算 `spec.clusters` 副本分配，RB/CRB 外层仍从 `status.schedulerObservingAffinityName` 对应的顶层 affinity term 开始；#5425 停滞于污染分支、compile/CI/DCO 和缺测试，不是功能方向被拒。v1.18 `overflowAffinities` 可在一个 term 内显式回到 primary，但只支持 Divided + Aggregated/dynamic Weighted；2026-07-06 的 #7717 用 Duplicated 再次复现，当前建议与 admission 支持矩阵冲突。Day 29 已交付中文证据报告及 1984x1611 彩色 `.mmd + PNG`，将 #5070/#5172 定位为 #7662 必须澄清但不能静默吸收的独立 scheduler contract；未发技术评论、未改代码，后续 `/assign` 见上一条。
- 2026-07-20：定位 PR [#7777](https://github.com/karmada-io/karmada/pull/7777) current head `dcd150b17` 唯一红灯：upstream `e2e test (v1.34.0)` [job 88263419523](https://github.com/karmada-io/karmada/actions/runs/29713746444/job/88263419523) 实际 273/273 specs、三个 Remedy cases 和组件 restart check 全通过；唯一硬失败发生在测试结束后的 `upload logs`，`CreateArtifact` 请求等待约 134 秒后报 `ETIMEDOUT`，下一项 kind artifact、同 run v1.35/v1.36 artifacts 及同 SHA fork v1.34 artifact 均成功。分类为新签名的 artifact-service/runner-network transient，底层两者无法区分；#7777 代码 `NO_FIX`，只需 `/retest`。该样本发生在 Day 27 滚动窗口结束后，已单独追加且未改旧分母。
- 2026-07-20：完成 upstream PR [#6863](https://github.com/karmada-io/karmada/pull/6863) 针对 issue #6861 的方案级 review。PR head `060724a5a1` 可无冲突合并到 `upstream/master@e4417e386`，scheduler、taint-toleration、cluster API 与 overridemanager 相关测试通过；但无条件在 `dynamicScaleUp` 按 Ready 清零会覆盖显式 `ClusterTolerations`，且健康容量直到 Select/overflow 之后才修正。临时回归稳定复现：unhealthy primary A 现有 4 + 估算 10、healthy overflow B 容量 10、desired 12 时，tier 0 先认领 12，随后 A delta 被清零并返回 `Clusters available replicas 0 are not enough to schedule`，B 未被尝试；`dynamicFreshScale` 也绕过新检查。用户审阅并确认新版 exact text 后，已发布一条含彩色 Mermaid 的自包含 [line review](https://github.com/karmada-io/karmada/pull/6863#discussion_r3612379211)；API 回读确认 author/head/path/right-line/body 全部一致，review state 为 `COMMENTED`，未附加总评或其他 PR 状态。
- 2026-07-20：将过去 72 小时 upstream PR E2E flake 复核追加到 Day 27，不改 Day 11/Day 26。独立滚动窗口为 `2026-07-17T03:19:27Z..2026-07-20T03:19:27Z`：88 个 pull-request workflows 中主 CI 22 runs，5 个 failure 里排除 1 个 codegen 后剩 4 个 E2E failed jobs；#7782/#7777/#7723 有 same-SHA success，为 E1 高置信 flake，并共同强化 control-plane / etcd stall 簇，#7779 是单次 Docker kill E0。当前无新增达到 E3+E4 的解决性 flake PR；#7782 timeout path 的 buffer data race 只列为独立 LIGHTWEIGHT helper hygiene，不误写成 etcd timeout 根因。旧 `83 runs / 23 runs / 29 jobs` 固定统计未累加重叠窗口。
- 2026-07-20：按用户授权 squash 并 force-push upstream PR [#7777](https://github.com/karmada-io/karmada/pull/7777)。先验证 7 月 18 日的 `13645be77` 仅采纳 Gemini 的泛型 `sets.New` + 空列表 fast path，随后补真实支持状态的终态 no-op 用例：新旧 `RemedyActions` 均为非空 `TrafficControl`、Conditions 相同时队列保持 0；临时关闭 set-equality 返回后精确失败 `queue length = 1, want 0`，恢复后普通/race remediation 包测试通过，`clusterEventHandler.Update` 本地覆盖率 100%。将原两提交与新测试 squash 为单个 signed-off commit `dcd150b17`，重写前后 tree 均为 `618f70c79e`；最新 `upstream/master@e4417e386` 未触碰两目标文件，merge-tree 无冲突。使用锁定旧 SHA `13645be77` 的 `--force-with-lease` 更新 fork/PR；API 回读确认 PR 仅一个 commit、两个文件、可合并，DCO 已通过，新 upstream CI 已启动。未改 PR body、未评论、未请求 reviewer。
- 2026-07-17：用户确认后完成 Remedy flake 的 upstream 发布。创建 [issue #7776](https://github.com/karmada-io/karmada/issues/7776)，发布 [`/assign`](https://github.com/karmada-io/karmada/issues/7776#issuecomment-5003434878) 并确认 assignee 为 `ranxi2001`；创建非 Draft [PR #7777](https://github.com/karmada-io/karmada/pull/7777)，目标 `master`，head `ranxi2001:fix/remedy-actions-reconcile@3861906f2`。发布前刷新确认 `upstream/master@1f07b77c3` 未移动、仅改两个 remediation 文件、无重叠开放 PR、exact-SHA fork CI 的 lint/codegen/compile/unit/v1.34-v1.36 E2E 全绿；发布后 API 回读确认 title/body/base/head/SHA/files/labels 均正确，DCO 已通过且 upstream CI 已启动。Issue 正文和 PR 正文与批准稿仅有 API 末尾换行差异；直接补 issue `kind/flake` 因账号缺少 `AddLabelsToLabelable` 权限失败，未擅自追加 `/kind flake` 评论。完整 exact text 与发布证据见 `internship-reports/day27-remedy-flake-upstream-drafts.md`。
- 2026-07-17：完成 Day 27 两项 flake 修复的 analysis/reproduction/fix 同维度闭环。将 upstream #7764 合并后的 `.claude/skills/e2e-root-cause-analysis/SKILL.md` 从 `upstream/master@1f07b77c3` 逐字复制到 repo-local intern skills，并用 `skill-creator` 新建 `.agents/skills/fix-e2e-flakes/`，规范 E3 实现门禁、同 causal edge 对齐、三段 Mermaid、最小 scope、E4 reverse-patch、nil/empty 与 event-loop 反证、fork CI 和 upstream approval；两个 skill quick validation 通过。Remedy branch `fix/remedy-actions-reconcile` commit `3861906f2` 只改 `eventhandlers.go/_test.go`：test-only baseline 精确失败 `queue length = 0, want 1`，修复与反向回退证明达到局部 E4；反向 review 将动作比较收敛为 set equality，并覆盖 nil/empty no-op。Migration branch `test/migration-binding-health-wait` commit `8ef8a08c6` 只改一份 e2e，把 RB one-shot GET 对齐为等待同一 `Applied && Healthy` 状态；package compile 通过。本机无 kind context，两个 commit 均已推 fork，四个有效 workflows 全绿，CI Workflow 的 lint/codegen/compile/unit 与 v1.34/v1.35/v1.36 E2E 全部成功；FOSSA/image-scanning 依 fork 条件 skipped。preflight 未发现阻塞 finding，未创建 upstream issue/PR/comment；下一步分别准备 concise exact target/text 并经用户确认。
- 2026-07-17：按用户纠偏重构 Day 27 的目标：不是只统计与原 PR 无关的红灯，而是筛选可通过 PR 消除的 flake。83 个 `CI Workflow` run objects 中严格纳入 23 runs/29 jobs（18 PR）；按 `READY / DONE / NEEDS_RCA` 分类后得到两个新候选：P0 Remedy 跨窗口三条新样本且 #7697 stale-cache/event-filter 链达到 E3，最小实现为 `eventhandlers.go` + focused regression；P1 migration 日志证明 Work health 与 ResourceBinding 聚合异步收敛，测试却在 Deployment ready 后单次读取 RB，最小实现为一文件复用 `WaitResourceBindingFitWith`，并补充英文时序 `.mmd + PNG`。#7732/Flink 已闭环；其余 25 jobs 缺共同 causal patch。#7723 artifact 证明 restart 来自 etcd readiness 失败、lease timeout 和 `leader election lost`，并回 control-plane 簇。Day 11/Day 26 未改动。
- 2026-07-17：完成 upstream PR #7697 针对性修复与 reviewer-facing 收尾。单 commit current head `bf24e47ce` 已推送：保留 persisted SAN、用 CA + client public key 绑定本地 kubeconfig、external-etcd credentials 改为 preserve-only、`--cert-mode=rotate` 保持 CLI-only，并用真实 API timeout 边界测试证明 11 个 Secret 部分写入后重跑可收敛；完整 CLI 测试、cmdinit lint、flags/import verifiers 通过。PR body 已同步当前契约；评论 `4851795706` 用内联 Mermaid 时序图替换过期 data-flow，评论 `4853917542` 从 573 词压缩为 165 词并区分 current-head guards 与早期 kind 恢复证据，两次 API 回读均与批准稿逐字一致。Day 26 保存完整方案、验证、批准与发布证据及 1450x1449 `.mmd + PNG`。最新 CI 快照为 14 success、`e2e v1.35` failure、`v1.34/v1.36` in progress；尚未归因代码，仍需分类后再请求 human review。
- 2026-07-17：对 upstream PR #7697 current head `3d1bc25b0` 做第二轮全 diff + 方案复审。确认 squash tree 与完成真实过期实验的 `4b6fa135f` 完全相同，current master 前进 9 commits 但未触碰 PR 路径，merge-tree 无冲突，17/17 checks success；在 exact head clean worktree 重跑完整 `go test ./pkg/karmadactl/... ./cmd/karmadactl/... ./cmd/kubectl-karmada/... -count=1` 通过。新发现两个 P1 correctness blocker：rotate 复用 install builder，从当前 Node 和执行机 `InternetIP()` 重建 SAN，远程/离线恢复即使重放全部显式 flags 也可能丢失旧隐式 SAN；本地 kubeconfig 只比较 CA，两个集群共享企业 CA 时会保留 A endpoint 却写入 B 的 `system:admin` 凭据。另有 external etcd blocking design decision：代码/测试允许替换 external etcd CA/client tuple 且不解析 key pair，和“root CA preserved/no CA rotation” scope 冲突。顺序更新因 same-CA + stable `karmada.key` 暂不升级为事务 blocker；v1.15-v1.18 cert/config Secret lists 相同，当前无 version-skew blocker。Day 13 已记录完整证据，状态从 `WAITING_HUMAN_REVIEW` 调整为 `REVIEW_BLOCKED`；pattern library 新增 persisted certificate identity 与 CA-not-cluster-identity 两条规则。未修改或发布任何 upstream 状态。
- 2026-07-17：经用户确认，在 upstream PR #7774 发布 conversation comment，依据 #7731 的 `[recovered]` 日志和 head `f416214b5` 中 controller-runtime 的 recover/rate-limit 路径，要求把 PR body 的 `operator crash` 收窄为 recovered reconcile panic；同时明确补丁保留原始 API error，但非法 Deployment 仍无法创建，CRD input validation 仍未解决。已用 GitHub API 回读确认评论身份、正文和链接：https://github.com/karmada-io/karmada/pull/7774#issuecomment-4998449728。未 `/assign`、未 request reviewer、未修改其他 upstream 状态。
- 2026-07-17：扫描 `karmada-io/karmada` 自 7 月 16 日以来 17 个更新的 open 条目和 6 个关闭/合并条目，并根据用户纠偏复盘候选价值判断。此前 gate 只证明 production reachability，没有判断是否值得投入，导致把 #7774/#7647 因“真实可达、CI 绿、无人 review”错误排为 A/B。补查确认：#7774 的非法 label 路径真实，但 controller-runtime 已 recover panic 并进入与普通 error 相同的 rate-limited retry，补丁只改善诊断，资源仍 stuck 且 CRD validation 未修；#7647 是用户显式传 `--etcd-pvc-size=abc` 的窄 CLI UX hardening。两者不再作为 review 候选，本轮正确结论是无干净新机会。#7764 新 head `7570842fb5` 仅合并段内 hard-wrap，既有 inference gaps 未变；#7697 仍为 `3d1bc25b0`，17 checks success、仅 Tide pending。`AGENTS.md` 固化 production relevance 偏好；`karmada-issue-discussion` 在 `thread_brief`/full JSON/diff/mock 前新增 Production Relevance Gate；`code-review-growth` 新增 Contribution Value Gate；pattern library 记录 `Reachable Edge Cases Are Not Automatically Valuable`。两个 skill quick validation 通过；第一轮 fresh-context test 正确将 invalid CLI 标为 `LIGHTWEIGHT`、mock extreme/recovered-no-outcome-change 标为 `SKIP`，并保留普通 token rotation 断连与跨租户 Secret 暴露为 `PRIORITIZE`；第二轮确认 relevance gate 只分配我们的注意力，不构成 merge veto：#7774 只值得纠正“进程 crash”的过强影响表述，#7647 无具体 finding 应保持不评论。Day 25/TODO 已撤回错误排序。未发布或修改 upstream 状态。
- 2026-07-16：完成本地中文学习图“ Karmada 资源传播、部署与调度组件详解”，用当前源码校准 `Resource Template + PropagationPolicy -> ResourceDetector -> Binding -> karmada-scheduler -> Binding Controller -> Work -> Execution Controller -> member kube-controller-manager/kube-scheduler` 主链，并区分资源到集群与 Pod 到 Node 两级调度、Push/Pull 执行位置及 Work/Binding 状态回传。交付 Day 24 报告、`.mmd`、可编辑 `.drawio`、嵌入式 SVG 和 PNG；使用现有 `agentcube-drawio-pr431` 容器内 draw.io `30.3.11` 转换，直接 PNG 导出因 `Empty export data` 失败，改用 Chromium 从最终 SVG 生成无裁切预览。同步补充 glossary，并纠正旧记录中“Execution Controller 生成 Work”的错误边界：Work 实际由 Binding Controller 生成。本轮未修改或发布 upstream 内容。
- 2026-07-16：复盘 upstream PR #7704 的维护者接管收尾过程。原 patch 在创建时合理包含 `release.yml` tar 替换与 `fossa.yml` Node 24 升级；Dependabot #7713 后续先合并 FOSSA 升级，使该文件因 base advancement 变得 redundant，而非一开始就 unrelated。维护者 `@RainbowMango` 代作者 rebase 并 force-push：`c180a4f` 已正确只剩 `release.yml`，但丢失 sign-off 导致 DCO action_required 和 checks cancelled；`e57d34d` 仅恢复 `Signed-off-by`，两者 tree 相同。随后 #7704 获 `lgtm/approved`，scope 过大的 #7703 被关闭。`code-review-growth` 新增 `Re-Audit Scope After The Base Branch Advances` pattern 和主 workflow gate；`karmada-pr-management` 要求延迟 review 时核对 current base/重叠 merged PR，区分 originally unrelated 与 now redundant，明确 rebase 后 residual diff，并复核 body/tests/sign-off/patch identity。本轮只读 upstream，未发布评论或改变 PR 状态。
- 2026-07-16：继续复盘 PR #7764 review 可读性：此前对 line comment 使用 Mermaid 过于吝啬，把分支原因、retry/control-flow 和多状态关系压成长 prose，增加作者认知负担。GitHub 官方文档确认 Mermaid 直接渲染于 pull requests/Issues/Discussions；`code-review-growth` 新增 Review Visualization Gate，3 个以上 actor/state/step、时序/retry、同一 signal 多原因或 current-vs-proposed 时默认比较 4-10 节点 inline Mermaid，格式为一句 finding -> 图 -> 一句 evidence boundary/action，并保留文字总结和 hypothesis 标记。`karmada-pr-management` posting/approval/reply gate 接入；`project-mermaid` 新增 inline review comment mode，one-off approved fence 可作为 canonical source。Day17 新增英文 fast-wait 与 retry `.mmd + PNG`，均用官方 Mermaid CLI 11.16.0 经 npx 白底渲染并原图检查，无裁切；后续评论应让图替换关系叙述而非叠加附件。Day22 safe-rescheduling 信息图作为正向先例：五阶段流降低关系推演成本，底部同时区分 supports、does not establish 和 ASR provenance；proposal 同流程节点变化保持相同顺序与标签，current/unchanged 中性、changed/new 强调色、open question 琥珀色、material risk 红色，并用标签或线型重复语义。新增 `proposal-change-template.mmd + PNG`，用官方 Mermaid CLI 11.16.0 验证基础 `classDef` 颜色；视觉检查后加入 subgraph 布局约束，current 固定在上、proposed 固定在下。相关 evidence-boundary 和 change-color 规则已同步到 skill。本轮未新增或编辑 upstream comment。
- 2026-07-16：对 PR #7764 review comment 做 post-review readability miss 复盘。作者明确表示 fast-wait 与 single-log-hit 两条难以理解；根因不是 `Could` 不够强，而是 comment 把 `signal -> claim -> counterexample -> missing evidence` 的关键桥压缩成 lifecycle/queue 术语，只有读过本地报告或聊天解释的人容易补全。`code-review-growth` 主 workflow 新增 Review Comment Comprehension Gate：line anchor 只提供位置，非平凡评论必须按 observation、concrete counterexample、reasoning、specific action 自包含，显式区分 observation 实际证明什么与文本声称什么，`Could` 只放在 action；pattern library 新增真实 miss。`karmada-pr-management` posting gate 接入该检查，并要求作者说不理解时从具体反例重写而非叠加术语。Day 17 已加入两条作者原话、缺失上下文表和重写草稿；均未发布，本轮未新增或编辑 upstream comment。
- 2026-07-16：复核 PR #7764 作者对 4 条 `ranxi2001` review 的完整回复和新 head `1972f0b4e`。`range-diff` 证明 patch 只删除 artifact layout 中错误的 `member1/2`，该项已修；compatibility E2E 仍使用双版本 artifact 名称，fast wait 仍被直接判 stale，single log hit 仍被直接判 no retry，flat component glob 仍不递归。作者对 ZIP 误报和 member layout 的处理正确，拒绝 `grep -E` 可接受；其余三条需用 `signal != diagnosis` 和 compatibility workflow 源码继续澄清。按用户要求为 hard-wrap 补充理论与官方实践证据：OpenAI 旧 `skills` 仓库已 deprecated 并重定向到 `plugins`，因此固定当前 `openai/plugins@11c74d6`（608 skills）和 `anthropics/skills@9d2f1ae`（17 skills），用 `markdown-it-py 3.0.0` default rules（CommonMark + table parsing）统计含 softbreak 的 paragraph；全库分别为 `1910/32956`（5.8%）和 `45/1375`（3.3%），OpenAI `openai-developers` bundle 为 `12/425`（2.8%）、Anthropic `skill-creator` 为 `1/170`（0.6%），PR 为 `21/35`（60%，47 softbreaks）。结合 CommonMark、Agent Skills parser、Claude lifecycle 和 Sclar et al. ICLR 2024，结论限定为 official-corpus-backed robustness convention；明确披露 OpenAI 当前 `plugin-creator` 也有 8/66 多行段落，不声称官方禁止 hard wrap 或每个换行必然降分。按用户确认仅向 hard-wrap thread 发布 [reply `3593371150`](https://github.com/karmada-io/karmada/pull/7764#discussion_r3593371150)；初次错误地沿用约 80 列排版，随后只合并段内 newline，最终正文 `168/4`，API 回读与本地 exact body 一致且 thread 保持 unresolved。Artifact scope、fast wait、retry 三条未发布；`code-review-growth` 已加入 fixed-SHA official-corpus baseline。Exact-SHA skill validation/diff check 通过；17 checks 中 15 success、v1.34 apiserver 503 与 v1.35 etcd timeout 两个 failure 当前仅为 E0，未归因 PR。
- 2026-07-16：按用户要求复核 PR #7764 新 push。首次刷新仍为 `ef572a249`，约两分钟后作者 force-push `44e708220`；新 commit 只是从 parent `d01d3a8fd3` rebase 到 `2f47894fa6`。`range-diff` 为 `=`、old/new stable patch-id 同为 `967d65395a3e`、两个 head 的 `SKILL.md` 逐字一致；直接 old-head..new-head 出现的 22 files 是 #7728/#7732/CodeQL base advancement，不是 PR patch。artifact scope/member layout、快速 wait stale 推断、flat component glob、single-hit no-retry 推断和 hard-wrap 均未修复；作者正确回复并 resolve 了 Copilot 的 job-text/run-ZIP 误报。exact-SHA skill quick validation、`git diff --check` 通过，DCO/codegen success，其余新 checks 仍运行。Day 17 已记录两次刷新、patch-equivalence 和未处理 finding；未发布 upstream 回复或修改 PR 状态。
- 2026-07-15：用户纠正第二场会议录像范围后，确认 `y-r0o2kDRXs` 为 `Karmada Bi-Weekly Meeting(Asia) for 20260630`、总长 `57:08`；下载 52.91 MiB 全量音频并以本地 `large-v3-turbo`/CUDA 做两轮转录。关闭 previous-text conditioning、加入 #7662 术语的第二轮产生 1193 cues，覆盖 `00:00:04.080-00:57:00.400` 且无 duration 越界；第一轮尾部到 `57:27` 的内容已判 hallucination。新增 Day 23 全量清理和明日对齐材料：Story 2 只有初步正面方向且 Descheduler/supported modes 未定；Story 3 在会上明确暴露 Policy/WR 双重真相、传播层 rolling、scheduler/controller 双写、unit ownership 和 cancel/rollback 未闭合。PR 唯一 head 是会议前 `2026-06-23` commit，至今未吸收会议反馈；未修改 upstream 状态。
- 2026-07-15：按用户要求用 `skill-creator` 新建全局 `~/.codex/skills/youtube-transcript-proofread/` 并同步到 repo-local `.agents/skills/youtube-transcript-proofread/`。skill 使用本地开源 `faster-whisper`，包含 URL/本地媒体转录、绝对 clip、manifest、SRT duration/重复/长 cue 校验、15 分钟 review chunks 和 Agent 校对证据规则；两份 skill quick validation、Python compile 和 SHA-256 文件对比通过。真实负例准确拦截第一场 16 个越界 cues；第二场 1193 cues 为 0 error/1 个已复核长 cue；15 秒绝对 clip 保持 `54:00-54:15`；fresh-context forward test 证明四个 packet 无遗漏、无重复。YouTube URL 复测遇到 anti-bot challenge，按安全规则未自动读取 cookies。Day 22 英文 16:9 信息图改从用户指定的 `Onefly-Content-Skills` 根目录安全读取 `.env`，经一次生成重试和两次 reference edit，修正多余分隔符、`policy` 误写及不存在的 `unit` API 字段；最终 `1672x941` PNG 已原分辨率视觉验收，无裁切或重叠。
- 2026-07-15：为 #7621 / #7662 补齐 2026-06-16 官方社区会议录像 `14:51-24:51` 的 ASR 证据。GitHub 安装 MIT `azuma520/youtube-to-notebooklm` 中的用户级 `whisper-transcribe` skill，建立独立 Python 3.12 / `faster-whisper` 环境，安装 FFmpeg/aria2，下载并校验本地 `large-v3-turbo` 模型；RTX 5080 Laptop GPU 完整转录成功。主转录末尾约 80 秒出现 previous-context 越界幻觉，截取后以 `condition_on_previous_text=False` 和 Karmada 术语 prompt 重跑，恢复到音频实际结束。新增 Day 22 清理稿，明确无 diarization、不可归因 maintainer、不可视为正式纪要；会议证据支持 WR 作为显式策略入口、当前 source 删除快于 target 创建会损害服务、A 等 B OK 再删的 target-first 不变量，以及 Pod/shard 不同迁移粒度，但不支持具体 API/state machine 已获批准。Day 15、README 和 TODO 已接入该证据；未修改 Go 代码或 upstream 状态。
- 2026-07-15：按用户提出的工具分工新建通用 repo-local skill `.agents/skills/project-mermaid/`：以 `.mmd` 为 canonical source，覆盖数据流、事件流、生命周期和时序图，默认渲染白底 PNG并保留 SVG/PDF 选项；提供 data-flow/sequence 模板、authoring/rendering references 和官方 `mmdc` wrapper。渲染默认只找已安装 `mmdc`，显式 `--backend npx` 才下载固定的 `@mermaid-js/mermaid-cli@11.16.0`，不静默上传内部图到 remote service；wrapper 可复用 PATH、`PUPPETEER_EXECUTABLE_PATH` 或 Playwright cache 中的 Chromium。`drawio-skill` 同步收窄为架构、拓扑、vendor icons、swimlane 和精细布局，并在 `.mmd` 是 canonical deliverable 时路由到 `project-mermaid`。模板 PNG、SVG、installed-mmdc/explicit-npx、非法语法清理均已验证；两个 fresh-context forward tests 分别生成 source-grounded data-flow 和 conflict/retry sequence，视觉检查无裁切，并据此补充 datastore read 和 sequence styling 规则。Day 21 和 `AGENTS.md` 已记录工具边界与验证证据。
- 2026-07-15：按用户要求新增 Day 21 绘图分析，纠正“Ubuntu 同时维护等价 draw.io/Mermaid”的粗略表述。证据确认 Day 15 两张报告 PNG/SVG 都带 Mermaid `flowchart-v2` metadata，是人工 `.mmd` 经 remote renderer 生成；`.drawio` 是同轮人工编写、经 validator 校验的可编辑 parallel source，当时 `drawio2mermaid.py` 尚未引入。视觉检查支持用户判断：current/proposed 对照、四层组件定位、语义色彩、共享状态 hub、风险隔离和行为型标签使两张图清晰；两个 `.drawio` 当前 `--strict --score` 均为 0 error/warning/crossing/overlap。报告将 visual quality 与 source maintainability 分开评价，并建议声明 canonical source/renderer；只有确需两种独立布局时才保留 curated parallel views。
- 2026-07-15：根据用户对“假想 bug”的复盘，给 `code-review-growth` 和 `karmada-issue-discussion` 增加 Production Reachability Gate：fault injection/mock 只能证明条件控制流，bug issue 或 blocking review 还必须证明真实 producer/interface 可产生触发条件、前置状态可由支持操作到达，并分析 retry/resync/restart/self-heal。证据分类统一为 observed bug、reachable latent bug 和 hypothetical scenario；后两项不得冒充已观察到的真实 incident。`concise-issue-writing` 和 review pattern library 同步加入约束。Fresh-context forward test 正确区分了真实 status Conflict 的 latent bug、被 admission 禁止的负数对象假想 bug，以及有 CI 503 日志的 observed bug。Day 20 将 #7623 更准确地记录为 API 写失败可达、但尚无线上 occurrence 的 latent correctness bug，并指出更强 regression 应使用真实 Conflict/stale resourceVersion，而不是只注入通用 error。
- 2026-07-15：按用户要求 squash upstream PR #7697。`/home/karmada` 的 `feature/cert-mode-rotate` worktree 在原 base `3d4d14d746` 上将 4 个 signed-off commits 折叠为 1 个 signed-off commit `3d1bc25b094f`（`feat: support rotating init-managed certificates`）；新旧 tree 均为 `988230d48330`，证明 10 files `+1696/-22` 的内容完全不变。用户确认 exact target/action 后，使用预期旧 head `4b6fa135fe87` 的 `--force-with-lease` 推送到 `ranxi2001/karmada:feature/cert-mode-rotate`；GitHub 回读确认 PR #7697 仅剩 1 commit、head 正确、DCO success，CI Workflow/CLI/Chart/Operator 新一轮 checks 已 queued/running。未修改 PR title/body/comments/reviewer requests。
- 2026-07-15：完成 upstream PR #7623 的定位和 controller 级失败恢复 review。该 PR 是 #7622 的窄 bug fix，与 #7621/#7662 proposal 不同；修复 stale target cache 的方向成立，但新增赋值在 executor rebuild 和 `Status().Update` 成功前提前提交新 target。隔离 worktree 的 fake client E4 复现证明：第一次 status update 失败后，第二次 reconcile 因 target unchanged 和 identical executor early return 而错误成功，status update 总调用数仍为 1、`ExecutionHistories` 为空；只删除新增 cache assignment 后第二次会重试，调用数变为 2。完整 `go test ./pkg/controllers/cronfederatedhpa -count=1` 通过，临时 worktree 已删除。用户确认通俗版 exact text 后，已在新增缓存赋值处发布 [`COMMENTED` blocking line review](https://github.com/karmada-io/karmada/pull/7623#discussion_r3584185187)；回读确认作者、head、path、line、正文和 review state 正确，未发总评或 maintainer mention。
- 2026-07-14：针对用户观察到的 Codex CLI 5-10 分钟无进展完成当前版本源码分析。官方 `openai/codex@5bed6447` 证明 HTTP/SSE 与 WebSocket 都将默认 `300000ms` stream idle timeout 用于请求后的首个事件和后续事件，默认 retry budget 为 5；WebSocket release 还隐藏第一次重连提示。本机 `codex-cli 0.144.4` 的只读结构化日志确认同一 HTTP/SSE turn 连续发生 `1/5`、`2/5` stream retry 后恢复，但没有把本机样本冒充 300 秒 causal proof。用户确认 exact target/title/body 后已发布 [openai/codex#33051](https://github.com/openai/codex/issues/33051)，聚焦独立 request-ack watchdog、可见等待状态和双 transport paused-time regression；自定义 endpoint 已从公开稿删除，四个 exact-SHA permalink 均返回 200。远端回读为 open/unlocked、无 assignee/milestone/comment，正文仅被 GitHub 添加一个结尾空行，自动标签为 `bug/CLI/custom-model/connectivity`。
- 2026-07-14：按用户要求将 repo-local `.agents/skills/drawio-skill/` 从 `v1.14.0` 升级到本轮内刚发布的官方稳定版 `v1.34.0@f8f4e89d`。最终 vendor 为官方 60 个 runtime 文件 + 本地 `agents/openai.yaml` + MIT `LICENSE`；Codex frontmatter 只保留 `name/description`，并明确 runtime-only 升级不安装上游 PR bot Action。官方 128 tests 全部通过、7 个因缺 draw.io/Graphviz/python-pptx 跳过；skill quick validation、所有脚本 compile、3 个历史图 `--strict` 和 Tube-Map smoke 均通过。审计同时证实 upstream `SKILL.md` top-level 已为 `1.34.0`、metadata 仍为 `1.19.0`，而 success 的 sync run `29328512589` 正读取旧字段，导致 `365-skills` marketplace 实际仍为 `1.19.0`。独立 commit `9b7105f` 修复 canonical version、fail-closed sync 和一致性测试，129 tests 通过；已推送 fork branch 并按用户确认创建 upstream PR [#94](https://github.com/Agents365-ai/drawio-skill/pull/94)。PR 回读为 open、非 draft、base/head 正确、4 files `+43/-7`、mergeable clean，唯一 `unittest` check success；下一步等待 maintainer review/merge 和 merge 后 sync run。
- 2026-07-14：用户确认 exact text 后，已在 upstream PR #7764 head `ef572a249` 一次性提交 [`COMMENTED` review #4692557177](https://github.com/karmada-io/karmada/pull/7764#pullrequestreview-4692557177)，四条 line comments 分别覆盖 artifact 名称只适配 base/scheduled scope、快速 wait 只能形成 stale-state hypothesis、单次日志命中不能证明没有 retry，以及约 80 列 prose hard-wrap 的 prompt-quality question。前三条由 workflow/source/artifact/queue 证据支持；hard-wrap 依据 Claude Code skill lifecycle、Agent Skills reference parser 和 Anthropic 官方 skills 写法，明确没有 Claude A/B 效果证据，因此为 non-blocking。首次 API 提交把转义串错误显示为可见文本，发现后立即只 PATCH 四条正文并逐条回读为用户确认版本；四个 thread 为 `3577478905/8907/8909/8914`。未发总评、maintainer mention、`lgtm/approve`，未修改 PR branch 或代码。
- 2026-07-14：重新核实 PR #7764 skill 的 member-cluster artifact 描述。枚举当前 head `ef572a249` 的全部 producer 后确认：`ci.yml`、`ci-schedule.yml`、`ci-schedule-compatibility.yaml` 中所有 `karmada_e2e_log_*` artifact 都由 `hack/run-e2e.sh` 收集，而该脚本只导出 host 和 pull-mode cluster（默认 `karmada-host`、`member3`）；#7719 run `28499042349` 的真实 artifact 与代码一致。本地执行同一脚本不会增加 `member1/2`；init/operator 使用不同 artifact 名称且各自只导出 host。因此 Day 17 第 6 条已从“待确认”升级为有效 finding，现有 Copilot thread 已覆盖，无需重复 upstream 评论；未修改或发布远端状态。
- 2026-07-14：用户确认 exact text 后，已在 upstream PR #7662 proposal line 569 发布 80 词 target-first review：https://github.com/karmada-io/karmada/pull/7662#discussion_r3576720182 。发布前复核 PR 仍为 open、head `586f6fc3508e`、目标行为位于 `SafeMigration` step 4，且 line 555-578 没有已有 thread；评论用当前代码路径指出 `Binding.spec.clusters` 更新会重触发 scheduler，临时 over-assignment 可进入 `dynamicScaleDown`，Duplicated/Static 也会完整重算，因此 source 可能在 `stableWindow` 前缩容。评论只请求作者选择 authoritative migration state 或 scheduler exclusion，并定义 source 不提前下降的 invariant；未 mention maintainer、未发布候选 B、未修改 PR 代码或 review state。GitHub 回读确认作者 `ranxi2001`、commit、path、line 和正文均正确。
- 2026-07-14：按用户要求使用 `$gpt-image-draw` / `gpt-image-2` 为 #7662 review 生成 16:9 中文技术信息图，三栏对应 `PreserveReady` 支持边界、target-first 与 Binding 调度冲突、direct deletion/finalizer 合同。首次原生图将 `GracefulEvictionTasks` 误写为 `GracefulExecutionTasks`；第一次参考图编辑未修正，第二次改为准确中文说明后成功。最终 PNG 已视觉检查，技术词和三栏关系正确；原始生成与修正提示词均保存在 Day 15 并接入报告索引。第三方图片编辑 API 响应较慢且执行会话在上下文切换时丢失回执，但产物已正常落盘。用户随后明确社区插图不得使用中文：现有图标记为 local-only，不可贴 upstream；Karmada 工作区后续所有新图默认英文，除非用户明确要求仅本地中文学习图。未修改 Karmada 产品代码或 upstream 状态。
- 2026-07-14：用户重申 #7662 proposal review 是本周主线。远端刷新确认 #7621/#7662 自上轮无增量：PR 仍为单 commit `586f6fc35`、720 行 proposal，9/9 bot threads unresolved，17/17 checks success，但无真人技术 review/LGTM/approve。按 `upstream/master@d0714678f` 完成 `PreserveReady` focused matrix：Duplicated `10/10/10`、Static `1/1/8` 均破坏 ready `4/4` 下界；Aggregated `6/4`、Dynamic `5/4/1` 只在 Steady 且全部 ready clusters 仍 eligible 时成立；Fresh 和过滤 ready cluster 均失败。源码和测试还证明 SafeMigration 不能只 patch `spec.clusters` 做 target-first：over-assignment 会触发 scheduler scale-down，同 patch 缩 source 则会被 binding controller 立即执行。独立 falsification 确认 direct delete 是 blocking gap：proposal 无 finalizer/deletion contract，当前 predicate 忽略 deletion，Binding 也不归 WR 所有。Day 15 已新增矩阵、causal chain、delete regression 和两条 100 词级候选 review；`code-review-growth` 新增 long-running deletion 与 multi-writer target-first 两条 pattern。未发布评论、未认领、未修改产品代码或 upstream 状态。
- 2026-07-14：按用户要求用 `RainbowMango`、`zhzhuang-zju`、`FAUST-BENCHOU`、`hzxuzhonghu` 的 Karmada authored issue/PR 历史增强简洁写作规范。GitHub API 核验 canonical accounts；剔除 HTML comments、空 body 和自动 cherry-pick 后，普通 PR visible-word 中位数分别为 80.5（n=24）、74（n=23）、38（n=21），`hzxuzhonghu` 仅 1 PR / 7 issues，明确只作定性参考。新增 Day 16 方法、代表链接、反例和 #7697 1015 -> 241 词回归；`karmada-pr-management` 新增 reviewer-attention gate，`karmada-issue-discussion` 新增 type-specific concise gate，两个 references 区分普通 PR、bug/flake、proposal/umbrella 和 comment；`draft_metrics.py` 可去 HTML comments、统计 visible words/nonblank lines 并在可选 soft limit 超限时失败。两个 skill 均通过 `quick_validate.py`；脚本在 #7697 新旧稿和 hidden-comment case 上通过，`--fail-over-limit` 正确返回 1；fresh-context API PR 输出 168 词并保留 compatibility/no-e2e，E2 flake 输出 184 词并拒绝 root-cause/fix claim。未发布或修改任何 upstream issue/PR/comment；当前 intern 文档/skill 改动尚未 commit/push。
- 2026-07-14：按用户反馈收缩 upstream PR #7697 的 reviewer-facing body。REST API 核验旧正文为 8414 bytes、120 行、约 1015 词，并含会过期的动态 CI 描述；当前 PR head 为 `4b6fa135f`，10 files `+1696/-22`，exact-SHA 17/17 checks 全部 success。用户确认 exact English text 后，已仅用 REST API 将 body 覆盖为 1821 bytes、30 行、241 词版本，只保留功能、默认兼容、安全/scope、关键测试、真实 10m leaf 过期恢复、AI disclosure 和 release note；文件表、完整 case 清单、CI 链接和实验时间线继续留在 Day 13。远端回读与本地候选逐字一致，SHA-256 为 `9740e0ca8750fe9f70fb04bc0ecea69d8d03ba55be055f1ce0d8da891cf535af`；未改 title、branch、commits、threads 或 reviewer requests。
- 2026-07-13：按用户要求建立 Day 15 高价值特性主线，完整尽调 upstream #7621 和直接 proposal PR #7662。确认 #7621 当前是无 assignee 的 `kind/question`，维护者明确“只讨论、暂不开发”；#7662 head `586f6fc35` 由 `@zhy76` 提交，`@RainbowMango` assign review，尚无人类技术 review/LGTM/approve。官方会议证据显示 2026-06-16 纪要原文为 `Openkruze-like workloads.`，2026-06-30 将 story 2 定位为 offline pending replica、story 3 定位为 online graceful migration，但没有记录设计批准。按 `upstream/master@d0714678f` 追踪 WR trigger、scheduler Fresh、Binding/Work、GracefulEviction、Descheduler、ResourceInterpreter 和多组件路径，结论是当前只有 failover-only 的部分 make-before-break，WR/Descheduler 都不保证 safe migration；#7662 仍缺 durable operation state、single-writer/concurrency、GracefulEviction 单一状态源、PreserveReady scheduling-mode 证据、AggregatedStatus freshness 和 API compatibility，且自动水位/比例/防振荡明确为 non-goal。新增 Day 15 报告以及 `.drawio`/Mermaid/PNG/SVG current-vs-proposed 图；draw.io/Graphviz 不可用，draw.io XML validator 为 `0 error / 0 warning`，Mermaid fallback 已视觉检查。未修改 Go 代码、未评论、未认领、未改变 upstream 状态。
- 2026-07-13：按用户“只增强有价值段落，不更新代码分支”的要求完成 repo-local skill 最小增强。`code-review-growth` 新增 claim-specific evidence labels（OBS/CODE/DOC/MAINTAINER/INFERENCE）、组件边界取证、terminal symptom 反向追因、read freshness 与 lifecycle freshness 区分，以及去锚定 independent falsification；pattern library 用 #7719/#7732 新增“fresh read 仍可能是 previous lifecycle state”。`karmada-pr-management` 仅新增高风险 differential review：base/head history、effect ledger/graph、revert-sensitive tests、coverage disclosure 和 finding labels，未修改 branch/push workflow。两个 skill 均通过 `quick_validate.py`，`git diff --check` 通过；三个 fresh-context forward tests 均命中预期且没有引入无关 branch 操作。Day 14 已记录吸收/拒绝项和 pinned source；没有安装或运行第三方 skill/script，没有修改 Karmada 源码或 upstream 状态。
- 2026-07-13：按用户要求检索知名开源维护者公开 Agent Skills，并使用 `skill-installer` 只读列出官方 curated catalog。通过 GitHub API 核验 Jesse Vincent `obra/superpowers@d884ae04`、Addy Osmani `agent-skills@98967c45`、Anthony Fu `skills@a74f281a`、Simon Willison `skills@a6579982`、Trail of Bits `skills@cfe5d7b1` 及知名组织仓库；逐项读取目标 `SKILL.md`、license、manifest、hooks 和 executable tree。当前 Karmada junior 首选是 `systematic-debugging`、`verification-before-completion`、`source-driven-development`，高风险 PR 再借鉴 `differential-review`；不整包安装，不因 star/作者身份跳过供应链审计。完整 shortlist、冲突点、固定 SHA、审计清单和未执行安装命令见 Day 14。本轮未安装或运行第三方 skill/script。
- 2026-07-13：复核 `@RainbowMango` 在 issue #7719 的 root cause analysis 及 PR #7732 最终状态。原始 job/component logs 证明：下一用例 `WaitCRDPresentOnClusters` 在 `07:29:51.516-51.525` 约 9ms 内命中上一轮 stale `APIEnabled`，而新 member CRD 到 `07:29:52.514` 才创建；status controller 在中间窗口采到 API 缺失，scheduler 唯一一次 scheduling 返回普通 `FitError`，随后 `ignoreErr=true -> err=nil -> Forget`；`07:30:02.212` 的 APIEnablements status-only 恢复不改变 generation，`updateCluster` 不 requeue binding，因此冻结到 420s timeout。纠正旧分析错误：presence helper 和 scheduler plugin 都读 `Cluster.Status.APIEnablements`，scheduler 不直接检查 member API。维护者据此 `/lgtm`、`/approve`，PR 于 `2026-07-13T07:24:54Z` 合并为 `d0714678`，issue 同步关闭。Day 11 已加入 observed facts、code-proven chain、Mermaid 和 causal edge；`code-review-growth`、`karmada-issue-discussion`、`karmada-pr-management` 已加入 E0-E4 flake RCA gate，禁止从 rerun/timing correlation 直接提出补丁。三个 skill 均通过 `quick_validate.py`；fresh-agent forward test 将 rerun + timing window + green CI 正确判为 E2，并拒绝 fix/endorse。本轮未执行 upstream-facing action。
- 2026-07-13：按用户要求对 PR #7697 final local head `4b6fa135f` 补做真实证书过期恢复测试。使用隔离 kind `v0.32.0` / Kubernetes `v1.36.1` 双节点 host cluster，以 `10m` leaf 安装并等待真实 `NotAfter`；过期后默认 kubeconfig `/readyz`、curl TLS、controller-manager/kube-controller-manager/scheduler 日志均复现 x509 expired。`100000h` CA 边界负例在任何写入前失败，Secret resourceVersion 和本地 kubeconfig hash 不变。随后用匹配原安装的 SAN/domain/etcd flags rotate 到 `8760h`，7 个 workload rollout 成功；最终 `/readyz=ok`、APIService `Available=True`、全部 workload desired/updated/ready `1/1/1`。机器断言证明三个 CA 和 `karmada.key` 不变，leaf 已更新，本地 kubeconfig 保留 `127.0.0.1:33443`/context/`0640` 且 client cert 与新 Secret 一致，过期前创建的 SA token 在恢复后仍 HTTP 200。完整时间线、失败命令和辅助脚本竞态见 Day 13；证据日志暂存 `/tmp/pr7697-expiry-final/logs/`。未 push、未评论、未改变 PR 状态。
- 2026-07-13：继续维护 XXL upstream PR #7697。先为 4 个 P1 添加失败回归测试，再在 `/home/karmada` 完成本地 hardening：保留兼作 ServiceAccount JWT signing key 的 `karmada.key`；刷新同一 CA 所属的本地 admin kubeconfig并保留 server/context/无关条目，跨集群 CA mismatch、损坏文件和 cert/key mismatch 均在 Secret update 前失败；对 root/front-proxy/internal-etcd CA 做 CA role、时间和 leaf lifetime 预检；修正原安装 flags 文档并断言 3-replica etcd、custom domain、external DNS/IP SAN。独立 review 补出跨集群 kubeconfig 污染风险并已加 CA guard；该漏审点已沉淀为 `Local Artifacts Must Be Bound To The Remote Target Before Refresh` review pattern。剩余 P2 是 external-etcd PEM/pair 校验、多 Secret 部分更新，以及原子 rename 不保留自定义 owner/group/ACL/xattrs。本地 signed-off commit 已创建并 rebase 到 `upstream/master@3d4d14d74`；线程审计后补 3 条 helper doc comments 并 amend 为 clean head `4b6fa135f`。range-diff 证明原 3 个有效 commit patch-equivalent并删除空 CI-trigger commit。最终聚焦测试、`go test ./pkg/karmadactl/cmdinit/... -count=1`（kubernetes `106.358s`）、目标 lint、command flags verifier 和 diff check 全部通过，rebase 后逻辑聚焦测试为 `37.823s`，amend 后 `TestValidateCAForLeaf`、lint/docs 再次通过。最终 `go test ./pkg/karmadactl/... ./cmd/karmadactl/... ./cmd/kubectl-karmada/... -count=1` 也通过（kubernetes `127.318s`），import-alias verifier 通过。远端仍为 `93eaf7e`，未 push、未执行 upstream action；下一步等用户批准 Day 13 的 lease-protected force-push。Day 13 是直到 merge 的唯一状态入口。
- 2026-07-13：复核 upstream PR #7732 的最新维护状态和 `WaitCRDDisappearedFromClusterStatus` 语义。`@RainbowMango` 于 `2026-07-13T03:00:03Z` 提交包含 `/assign` 的 `COMMENTED` review，GitHub event 确认他已把自己设为 PR assignee；当前 requested reviewers 仍为 `@Vacant2333` 和 `@XiShanYongYe-Chang`，尚无 `lgtm/approve`。helper 每 5 秒读取 Karmada `Cluster` 状态，最长 420 秒，直到目标 FlinkDeployment GVK 不再被 `Cluster.Status.APIEnablements` 报告为 `APIEnabled`；`APIDisabled` 和 `APIUnknown` 均可收敛，与 scheduler 只允许 `APIEnabled` 集群的语义一致。这是等成员集群 CRD 对象删除之后的第三层状态收敛 barrier，防止下一个 e2e 用例读到旧 API 能力缓存。
- 2026-07-13：按用户“先推进 flake 相关 issue”的要求完整 review upstream PR #7692。补丁在第一个 ClusterResourceBinding e2e `It` 结束前增加 member `ClusterRole` present barrier，解决旧 cleanup 把“尚未传播”的首次 `NotFound` 错当成“已清理”的竞态。独立失败产物 run `28397071031` / job `84140026710` 显示 cleanup 于 `19:57:47.708` 开始，`member2` 直到 `19:57:48.083` 才创建目标 ClusterRole，与 #7691 描述完全一致。`git diff --check`、e2e package compile test 和当前 upstream CI 通过；无阻塞性 finding，但单次 CI 不是 flake 消失的统计证明。用户确认 exact text 后已发布 [comment review #4681354181](https://github.com/karmada-io/karmada/pull/7692#pullrequestreview-4681354181)；GitHub API 回读确认作者 `ranxi2001`、状态 `COMMENTED`、正文和失败时序完整。通用经验“异步 cleanup 中只有先观察到 presence，后续 absence 才能证明真正删除”已补入 `code-review-growth` pattern library。
- 2026-07-13：用户确认最终原文后，已在 upstream issue #7757 发布复现/review 评论：https://github.com/karmada-io/karmada/issues/7757#issuecomment-4953788174 。评论基于 `upstream/master@3d4d14d74`，说明 `AllocatableModeling.Count` 的 `expected=1 actual=0` 是 resource-model node count、不是 Pod slot 数；记录 `break -> continue` 后临时回归测试和现有相关单测均通过，限定影响为相关动态副本估算路径，建议顺序不变性/all-saturated 测试，并按用户要求 `cc @RainbowMango` 请求维护者确认语义。GitHub API 回读确认评论作者 `ranxi2001`、目标 #7757、正文和 mention 正确；Day 12 已从草稿改为发布记录。
- 2026-07-13：按用户要求简单 review upstream issue #7757 并核对作者公开贡献背景。源码确认 `getNodeAvailable()` 的 `nil` 只表示单个节点 Pod slot 已满，而调用方 `break` 会终止整个 modeling 循环；client-go cache 的 map 遍历无顺序保证，因此结果会随节点顺序变化。独立临时 worktree 回归测试在当前代码得到 `expected=1 actual=0`，仅改 `break -> continue` 后通过，相关现有单测也通过；临时 worktree 已删除。影响限定为使用 `ReplicaRequirements` 的动态副本估算路径，不是所有调度。作者是 Karmada 新贡献者、尚无 merged Karmada PR，且 #7638/#7643 的原始 bug 判断均未成立，因此应提高其 PR 的证据要求；但 #7757 本身已有独立证据支持。详细 review 已补入 Day 12，循环控制经验补入 `code-review-growth` pattern library；评论随后已按用户确认发布，见上一条。
- 2026-07-13：扫描 `karmada-io/karmada` 最近 30 天的 40 个 open issues 及近期 open PR，逐项核对 assignee、`/assign`、关联 PR、真人/bot review 与 CI。结论是当前没有“维护者已确认、无人认领、无 active PR”的干净新实现项；优先级收敛为：先推进自有 PR #7732，再回复直接 `@ranxi2001` 的 #7757，随后 review #7692 或 #7754，最后清理 #7697 已处理的 bot threads 并追真人 review。#7757 源码证据确认 `getAllocatableModelings()` 在饱和节点执行 `break` 会遗漏后续健康节点，但作者已认领，不重复实现。完整候选、排除理由和 #7757 英文草稿记录在 `internship-reports/day12-new-issue-pr-opportunities.md`；本轮未执行 upstream-facing action。
- 2026-07-10：按用户要求重构 `internship-reports/day11-ci-flake-statistics.md`，把“一页结论 / 最新关注 / 高置信 Flake / 持续关注台账”放在正文前半部，将统计口径、rerun/trigger 样本、PR #7732 文案与技术复查、待补日志、排除项和数据采集命令下沉到附录。最新 GitHub 快照显示 #7732 仍 open、无 human review，核心 lint/codegen/compile/unit/e2e v1.34-v1.36 通过；此前 cancelled 的 Chart/Operator workflows 已进入 attempt 2，当前各有一个 job pending，Tide 仍等待 `approved`/`lgtm`。TODO 已同步改为等待 rerun 收尾与 human review。
- 2026-07-10：继续清理遗漏的 #7719 issue body 临时副本。第二次 FlinkDeployment timeout、#7697/#7728 空提交与 `files=0` 证据已在 Day 11 归档，Day 9 已补最终 issue 更新索引，因此删除独立临时 body 文件并继续使用 Day 9/Day 11 作为本地入口。
- 2026-07-10：按用户要求整理 `internship-reports/` 下以 `pr` 开头的 8 个临时文件。#7697 最终 scope comment 合并回 Day 8；#7719/#7732 的设计、最终验证和代码复查合并回 Day 11，Day 9 删除过期 PR body 草稿并改为结果索引；#7728/dashboard/website 的最终 body、验证和 PR 状态统一保留在 Day 10，删除重复旧草稿；README/TODO/PROGRESS 均改为 Day 文档入口。整理后不再保留独立 `pr*` body/plan/review 文件。验证时本机没有 `ruby`，改用 Perl 完成 Markdown fence 检查；文件名扫描、删除文件引用扫描、本地 Markdown 链接检查和 `git diff --check` 均通过。
- 2026-07-10：完整复查 upstream PR #7732 当前 head `1240559dd`。读取 PR/issue body、全部 issue/line review/review、4 文件 diff、相关 helper/APIEnablement 实现、Ginkgo 并行/Serial 配置和最新 checks；确认仓库内 4 个 FlinkDeployment CRD 创建路径均覆盖 control plane CRD、member CRD、Cluster.Status.APIEnablements 三段 cleanup barrier，DeferCleanup LIFO 顺序正确，`APIUnknown` 不会让下一轮 setup 误判 ready，未发现 correctness finding。重新执行 `git diff --check upstream/master...upstream/pr-7732` 和 `go test ./test/e2e/framework ./test/e2e/suites/base -run '^$' -count=0` 均通过，topic worktree 干净且 Signed-off-by 存在。upstream lint/codegen/compile/unit/e2e v1.34-v1.36 通过；Chart v1.34 与 Operator v1.35 各一个 job 仅标记 `The operation was canceled.`，同矩阵其他版本和 fork workflows 通过，归类为 CI/process interruption，不是代码 finding。评审结论已合并到 `internship-reports/day11-ci-flake-statistics.md`，下一步等待 human `lgtm`/approval；未执行任何 upstream-facing action。
- 2026-07-10：用户确认后已在 upstream PR #7732 的 Gemini line review thread 回复，链接 https://github.com/karmada-io/karmada/pull/7732#discussion_r3555941597 。回复说明已核对 Gomega `Eventually(func(g gomega.Gomega) ...)` 语义和 Karmada 现有 e2e helper 风格，`g.Expect(err).NotTo(gomega.HaveOccurred())` 失败会停止当前 poll 并让 Eventually 重试，不会继续执行到 nil `cluster.APIEnablement(...)`；除非 maintainer 更偏好显式返回 error，否则保持当前风格。
- 2026-07-10：验证 PR #7732 中 Gemini line comment 是否为真实 bug。使用临时测试 `test/e2e/framework/gomega_eventually_temporary_test.go` 复现 `Eventually(func(g gomega.Gomega) ...)` 控制流：当 `g.Expect(err).NotTo(HaveOccurred())` 失败时，测试输出 `continuedAfterFailedAssertion=false`，失败停在 assertion 行，没有执行到后续 nil dereference；另验证返回 `(false, err)` 的建议写法可以正常重试并通过。结论：Gemini 评论中的 nil pointer panic 判断不成立；返回 error 只是更显式的风格改进，不是必须修复的真实 bug。临时测试文件已删除，`/tmp/karmada-7719-flink-flake` 工作区干净；该复盘已补入 `code-review-growth` pattern library。
- 2026-07-09：用户确认后已创建 upstream PR [karmada-io/karmada#7732](https://github.com/karmada-io/karmada/pull/7732)，标题 `test(e2e): wait for FlinkDeployment CRD cleanup`，目标 `karmada-io/karmada:master <- ranxi2001:test/flinkdeployment-crd-cleanup`。PR 为 open、非 draft，base/head、正文和 commit `1240559dd34cc0eedd0ec6cffe97b5c0076660dc` 均核对正确；DCO 已通过，`kind/flake` label 已生效，tide pending 需要 approved/lgtm，CI Workflow / Chart / CLI / Operator 初始 checks 正在 queued/pending。
- 2026-07-09：为 #7719 FlinkDeployment e2e flake 修复做 upstream PR 最终 preflight。`/tmp/karmada-7719-flink-flake` 当前在 `test/flinkdeployment-crd-cleanup`，本地 HEAD 与 `origin/test/flinkdeployment-crd-cleanup` 均为 `1240559dd34cc0eedd0ec6cffe97b5c0076660dc`，并且 `upstream/master` 是 HEAD 祖先。重新核对 `git diff --check upstream/master...HEAD`、`go test ./test/e2e/framework ./test/e2e/suites/base -run '^$' -count=0` 均通过；open PR 搜索未发现 #7719 重复 PR；fork push Actions 为 CI Workflow / Chart / CLI / Operator success，FOSSA / image-scanning skipped。下一步等待用户确认 exact target/title/body 后创建 upstream PR。
- 2026-07-09：按用户反馈重新梳理 `internship-reports/day11-ci-flake-statistics.md`，把前半部分从冗长统计口径改为“目标与结论 / 简化口径 / 关键数字 / Rerun-Trigger 证据”，将 GitHub 命令和中间文件下沉到附录。整理后 Day 11 开头直接给出：598 条 upstream run、32 条 failed run、56 行 e2e/setup/Kubernetes test 非成功 job、3 类高置信 flake、4 个 upstream 高置信样本、1 个 fork CI 补充样本。期间 `test/flinkdeployment-crd-cleanup` fork CI run `29006012630` attempt 2 完成，失败的 v1.34 job `86098566248` 已通过；最终 fork push CI 为 `2 skipped, 4 success`。#7719 的设计和 PR 验证结果现统一归档在 Day 11 与 TODO。
- 2026-07-09：用户询问当前 `test/flinkdeployment-crd-cleanup` v1.34 fork CI 控制面失稳问题是否已记录到 Day 11。已确认 Day 11 原本已有 “Aggregated API / control plane 短暂不可用导致多用例连锁失败” 类别，但未包含这次个人 fork push CI 样本。已把 `run 29006012630 / job 86080188511` 作为“不计入 upstream 598 条 run 总量统计”的补充样本追加到 `internship-reports/day11-ci-flake-statistics.md`，记录 `etcdserver: request timed out`、API `connection refused`、`leader election lost`、多控制面容器退出以及 FlinkDeployment cleanup 属后续连锁超时。
- 2026-07-09：排查 `test/flinkdeployment-crd-cleanup` commit `1240559dd34cc0eedd0ec6cffe97b5c0076660dc` 的 fork push CI 失败。Chart / CLI / Operator 均通过；CI Workflow 中 lint、codegen、compile、unit test、e2e v1.35.0、e2e v1.36.1 均通过，只有 e2e v1.34.0 job `86080188511` 失败。失败时间线显示最早硬失败是 `09:26:51` 的 `[Suspension] ClusterPropagationPolicy testing [It] suspend the CPP dispatching`，`UpdateClusterPropagationPolicyWithSpec` 返回 `etcdserver: request timed out`；此前 artifact 中 `09:26:12-09:26:19` 已有多个控制面容器退出，`karmada-controller-manager` 记录 `leader election lost`，后续大量 Karmada/member API `connection refused`。`ScheduleMultiTemplate` FlinkDeployment cleanup 在新增 member CRD wait 处超时发生在控制面失稳之后。当前判断为 v1.34 e2e 环境整体 flake，不是 #7719 修复代码的确定性失败。已执行 `gh run rerun --repo ranxi2001/karmada --job 86080188511`，run `29006012630` 进入 attempt 2，新的 e2e v1.34 job 为 `86098566248`；后续 attempt 2 已通过，最终 fork push CI 全绿。详细证据现统一归档在 Day 11 与 TODO。
- 2026-07-09：开始准备 #7719 FlinkDeployment e2e flake 修复 PR。按 design-before-code 流程确定 4 文件 scope，从最新 `upstream/master@3d4d14d74` 创建干净 worktree `/tmp/karmada-7719-flink-flake` 和分支 `test/flinkdeployment-crd-cleanup`，cherry-pick 候选修复为 commit `1240559dd34cc0eedd0ec6cffe97b5c0076660dc`（标题 `test(e2e): wait for FlinkDeployment CRD cleanup`）。本地验证 `git diff --check` 与 `go test ./test/e2e/framework ./test/e2e/suites/base -run '^$' -count=0` 均通过。分支已推送到 `origin/test/flinkdeployment-crd-cleanup`，fork push CI 已触发：`CI Workflow` run `29006012630`、`Chart` `29006012662`、`CLI` run `29006012583`、`Operator` run `29006012840`；设计和最终 PR 状态现统一归档在 Day 11。
- 2026-07-09：用户确认后已更新 upstream issue `karmada-io/karmada#7719` body，GitHub 返回 https://github.com/karmada-io/karmada/issues/7719，`updatedAt=2026-07-09T08:37:26Z`。新正文保留原有 FlinkDeployment cleanup/APIEnablements race 分析，并补充两条新证据：#7728 merge 后 master push run `28998390044` / job `86054168911` 在 `EstimatorAssumption NodeResource` FlinkDeployment 用例中同样因 `ResourceBinding` not found / `resourcebinding.go:47` timeout 失败；#7697 的 `93eaf7e` 与 #7728 的 `de3b6be` 均是 `test: trigger ci` 空提交，commit stats 为 `files=0/additions=0/deletions=0`，后续 PR CI 全绿。已用 `gh issue view` 验证正文包含 `28998390044`、`86054168911`、两个 trigger commit SHA 和 `files=0`；最终证据现归档在 Day 9/Day 11。
- 2026-07-09：补充 Day 11 CI flake 专项里的 “rerun / trigger CI 转绿统计”。Actions REST API 显示统计窗口内 598 条 run 中有 10 条 `run_attempt > 1`：`failure -> success` 2 条（`run 28212061472` CLI workflow 的 v1.35/v1.36 Kubernetes test、`run 28256528352` CI Workflow 的 v1.36 e2e，第二次 attempt 均无失败 job）、`action_required -> success` 4 条、release `failure -> failure` 3 条、`failure -> cancelled` 1 条。另确认空提交 trigger CI 高置信样本 2 个：#7697 的 `93eaf7e` 和 #7728 的 `de3b6be` 均为 `test: trigger ci` 空提交，GitHub commit API 显示 `files=0/additions=0/deletions=0`，后续 PR CI Workflow 及 Chart/CLI/Operator matrix 直接全绿。
- 2026-07-09：新增 `internship-reports/day11-ci-flake-statistics.md`，完成 mentor 要求的 CI flake 专项初版。统计窗口为 `2026-06-26` 到 `2026-07-09`，覆盖 598 条 GitHub Actions run，其中 failure 32 条；展开后非成功 job rows 72 条，e2e/setup/Kubernetes test 类 56 条。高置信 flake 分三类：FlinkDeployment estimator ResourceBinding timeout（关联 open issue `#7719`，也命中 #7697 和 #7728 merge 后 master push）、Remedy `TrafficControl` cleanup timeout（关联历史 closed issue `#5323`）、aggregated API / etcd transient 503（#7728 PR CI v1.35，空提交 trigger 后全绿）。已把 #6841/#7388/#7719/#7691/#7692/#5323/#3667/#3682/#3699/#5263/#4427/#7697/#7728 作为相关 PR/Issue 台账整理进报告，并更新 `internship-reports/README.md` 和 `internship-reports/todo.md`。
- 2026-07-09：排查已合并 PR `karmada-io/karmada#7728` 的 master push CI 失败。merge commit `3d4d14d746de507164abf40c1017b1f2b0e47e3a` 的 `CI Workflow` run `28998390044` 只有两个 e2e matrix job 失败：`e2e test (v1.36.1)` job `86054168903` 失败在 Remedy `test with nil decision matches remedy / Create an immediately type remedy, then remove it`，删除 Remedy 后等待 `Cluster.Status.RemedyActions` 清掉 `TrafficControl` 超时，关联历史 flake `#5323`；`e2e test (v1.34.0)` job `86054168911` 失败在 FlinkDeployment estimator assumption 流程，等待 ResourceBinding 出现/进入调度结果超时，关联已 open 的 flake issue `#7719`。同一 run 的 lint/codegen/compile/unit/e2e v1.35 通过，其他 merge-triggered workflows 也通过；PR head `de3b6be675` 的 upstream PR CI 和 fork push CI 均全绿。当前判断是合并后 master CI 命中两个 e2e flake，不是 Ubuntu 24.04 runner 升级的确定性失败；建议先重跑 failed jobs。详细证据已补入 `internship-reports/day10-ci-ubuntu-runner-upgrade.md`。
- 2026-07-09：根据报告图片重命名后的链接变化，先尝试把 upstream PR `karmada-io/karmada#7697` 的既有评论 `#issuecomment-4851795706` 图片 raw URL 改为新文件名；随后确认 GitHub 评论不适合依赖 fork 内 raw 图片链接，用户已在网页评论框直接上传图片。GitHub API 验证该评论当前使用 `https://github.com/user-attachments/assets/2b9c0efb-2fad-4f93-b05e-df9e4cdfeba1`，`updated_at=2026-07-09T01:17:31Z`。最终发布文本现归档在 Day 8。
- 2026-07-08：整理 `internship-reports/` 图片文件命名，把 6 张无 day 前缀图片重命名为 `day2-...`、`day3-...`、`day8-...` 格式，并同步更新 Day 3 / Day 8 报告里的本地 Markdown 链接和 raw GitHub 图片 URL。已补充 `AGENTS.md` 规则：报告图片和导出视觉资产必须使用匹配报告的 `dayN-` 前缀。
- 2026-07-08：复查 `volcano-sh/agentcube#400` PicoD metrics PR 当前 head `1f0af1c8158735ae26b9debdf752f6277ad3ab65`。本地 clone `/tmp/agentcube-pr400`，`go test ./pkg/picod -count=1`、`go test -race ./pkg/picod -count=1`、`go test ./pkg/picod -run 'TestMetrics|TestServer_MaxBodySizeMiddleware|TestNewServer_JWTMode_RequiresAuth' -count=1` 和 `go test ./pkg/picod -run TestMetrics_Exposition -count=50` 均通过。新增临时 panic route 测试验证 maintainer 评论：当前顺序 `Logger -> Recovery -> Metrics -> maxBody -> gzip` 下，panic 被 `Recovery()` 转成 500 后不会回到 metrics 的 post-`c.Next()` 记录逻辑，因此 `GET /panic status_code=500` 缺失；临时把 metrics 放到 `Recovery()` 前后同一测试通过。另注意 `metrics_test.go` 里 `SampleSum > 0` 属低概率 flaky 断言，已补入 `.agents/skills/code-review-growth/references/review-patterns.md`。
- 2026-07-08：按用户要求把代码 review 能力提升流程沉淀为本地 skill `.agents/skills/code-review-growth/`，并通过 `quick_validate.py`。该 skill 要求 review 前读取 PR body、diff、CI、普通 comments 和 line review comments，先建立变更模型和风险面，再逐项检查正常路径、提前返回、panic/recovery、timeout/cancel、cleanup、observability 和测试覆盖；已把 AgentCube #400 的 Gin middleware metrics 漏检复盘写入 `references/review-patterns.md`，包括 metrics 必须包住 `gin.Recovery()`、body-size abort、Prometheus label cardinality 和 metric 语义边界。
- 2026-07-08：根据用户发现的 Karmada GitHub workflow Ubuntu runner 版本问题，调研上次 runner 升级 PR #3699：当时为解决 #3667 kind/e2e CI flake，将多处 workflow 从 `ubuntu-20.04` 统一升到 `ubuntu-22.04`。确认 GitHub runner-images 已公告 `ubuntu-22.04` 将于 2026-09-17 开始 deprecation、2027-04-17 unsupported；Ubuntu 24.04 LTS 标准安全维护到 2029 年 5 月，GitHub hosted runner 侧当前是 GA image。已从最新 `upstream/master@d01d3a8fd` 在独立 worktree `/tmp/karmada-ci-ubuntu24` 创建分支 `chore/update-github-runner-ubuntu-24`，提交 `0f62fd62b ci: update GitHub runners to Ubuntu 24.04`，把 `.github/workflows` 下 18 个文件共 30 处 `ubuntu-22.04` 统一替换为 `ubuntu-24.04`。本地 `git diff --check`、无残留 `ubuntu-22.04`、Python YAML parse 通过；`actionlint` 本地未安装。分支已推送到 `origin/chore/update-github-runner-ubuntu-24`，fork push CI commit `0f62fd62b05802961447601da9000403139b600d`：Chart/CLI/Operator 已 passed，CI Workflow 的 lint/codegen/compile/unit test 已 passed，主 e2e 预计耗时较长仍在 running，用户要求先不等待。已新增 `internship-reports/day10-ci-ubuntu-runner-upgrade.md`，并在用户确认标题/body 后创建 upstream PR [#7728](https://github.com/karmada-io/karmada/pull/7728)；PR DCO 已通过，upstream CI 正在运行。
- 2026-07-08：跟进 upstream PR #7728 CI，当前只有 `CI Workflow / e2e test (v1.35.0)` 失败，job `85774432012` / run `28912823833`；同一 PR 的 lint/codegen/compile/unit、e2e v1.34/v1.36、Chart/CLI/Operator 全部通过。失败表现为 `karmadactl exec` 超时、`karmadactl get` 和 `SynchronizedAfterSuite` 读 `clusters.cluster.karmada.io` 返回 `503 ServiceUnavailable`。artifact 日志显示 `02:51:14` 起 kube/karmada apiserver 出现 etcd `DeadlineExceeded` / handler timeout，两个 `karmada-aggregated-apiserver` 副本 `02:51:38` 停止 serving，`02:52:21/02:52:24` 重新启动并恢复 `cluster.karmada.io v1alpha1`。另有 `test/e2e/framework/karmadactl.go:148` 的 timeout 错误拼接路径触发 race detector 报告。当前判断：更像 e2e/control-plane flake 或 CI 资源压力下的 aggregated API 短暂不可用，不是 workflow runner label 机械替换引入的确定性代码问题；建议先 rerun failed job，若无权限再经用户确认推 signed-off 空提交重触发 CI。
- 2026-07-08：尝试直接重跑 PR #7728 失败 job：`gh run rerun --failed` 和 `gh run rerun --job 85774432012` 均被 GitHub 拒绝；REST API 返回 `403 Must have admin rights to Repository`。当前账号不能直接重跑 upstream job。替代方案：请维护者 rerun，或经用户确认后向 `origin/chore/update-github-runner-ubuntu-24` 推 signed-off 空提交触发新一轮 PR CI。
- 2026-07-08：用户确认后，已在 `/tmp/karmada-ci-ubuntu24` 的 `chore/update-github-runner-ubuntu-24` 分支创建并推送 signed-off 空提交 `de3b6be675 test: trigger ci` 到 `origin/chore/update-github-runner-ubuntu-24`，用于重触发 PR #7728 CI。PR head 已更新为 `de3b6be675bbf8ad12f91052f7d0fb53c5b592a5`；upstream PR checks 已 queued/in_progress。fork push CI 同步触发：`CI Workflow` run `28915975049`、`Chart` `28915975027`、`CLI` `28915975040`、`Operator` `28915975026`、`FOSSA` `28915975012`，`image-scanning` `28915975025` skipped。下一步重点观察 upstream `CI Workflow / e2e test (v1.35.0)` 是否复现。
- 2026-07-08：核实原实现提交 `0f62fd62b05802961447601da9000403139b600d` 的 fork push CI 已全完成：`CI Workflow` passed，包含 lint/codegen/compile/unit/e2e v1.34/v1.35/v1.36；`Chart`、`CLI`、`Operator` 的 Kubernetes v1.34/v1.35/v1.36 矩阵均 passed；`FOSSA` 和 `image-scanning` skipped。已在 `internship-reports/day10-ci-ubuntu-runner-upgrade.md` 追加更新后的 PR body 草稿，用于把当前 PR body 里 “e2e jobs are still running” 改为最终 fork CI 结果，并说明 `de3b6be675` 是空提交重触发 CI。
- 2026-07-08：用户确认后已更新 upstream PR #7728 body。`gh pr edit` 因 token 缺少 `read:org` scope 被 GraphQL 拒绝；随后用 REST API `PATCH /repos/karmada-io/karmada/pulls/7728` 成功更新。PR body 现在包含 `0f62fd62b` 的完整 fork push CI passed 结果，并说明 `de3b6be675` 是 signed-off empty commit，只用于重触发 PR CI。
- 2026-07-08：PR #7728 收到 `@RainbowMango` 维护者评论，感谢 runner 更新并邀请继续处理 `karmada-io/dashborad` 的 Ubuntu 22 workflow。核对后确认 `dashborad` 是拼写错误，实际仓库是 `karmada-io/dashboard`，默认分支 `main`；其中 6 个 workflow 文件仍有 18 处 `runs-on: ubuntu-22.04`。当前没有相关 open PR/issue，且 `ranxi2001/dashboard` fork 不存在。建议单独做 dashboard PR，不混入 #7728；如继续，先 fork dashboard、机械替换为 `ubuntu-24.04`、跑 fork push CI，再开 upstream PR。可回复维护者：`Thanks for pointing this out. I checked that the remaining ubuntu-22.04 usages are in karmada-io/dashboard; I will handle them in a separate dashboard PR.`
- 2026-07-08：按用户要求把 dashboard 和 website 分开准备 PR，并先 fork。已创建 `ranxi2001/dashboard` 和 `ranxi2001/website` fork。dashboard worktree `/tmp/karmada-dashboard-ubuntu24`，branch `chore/update-github-runner-ubuntu-24`，commit `8f6ba046914e3e1bcc3a4d94f33912c10e33c64f`，6 个 workflow 文件 18 处 `ubuntu-22.04` -> `ubuntu-24.04`，本地 grep/diff-check/YAML parse 通过，fork push CI `CI Workflow` run `28917428886` passed，包含 lint/unit/build/e2e-frontend v1.33/v1.34/v1.35。website worktree `/tmp/karmada-website-ubuntu24`，branch `chore/update-github-runner-ubuntu-24`，commit `24e7dd4515225a9462b47e04a7ca79285a586964`，2 个 workflow 文件 2 处替换，本地 grep/diff-check/YAML parse/CRLF 检查通过，fork push CI `Typos Check` run `28917428642` passed；`CRLF Check` 仅 pull_request 触发。后续 PR 创建结果和最终验证现统一归档在 Day 10。
- 2026-07-08：用户确认后已创建两个 follow-up PR：`karmada-io/dashboard#643` 和 `karmada-io/website#1036`，标题均为 `ci: update GitHub runners to Ubuntu 24.04`。Dashboard PR DCO passed，upstream `CI Workflow` 和 `CodeQL` 正在运行；Website PR DCO passed，`CRLF Check` passed，`Typos Check` 和 Netlify checks 正在运行。主仓库 `karmada-io/karmada#7728` 新一轮 PR CI 已全绿：lint/codegen/compile/unit/e2e v1.34/v1.35/v1.36、Chart/CLI/Operator 全部 passed，DCO passed。尚未回复 `@RainbowMango` 评论；可回复草稿见 Day 10。
- 2026-07-06：按用户反馈修正 upstream issue #7719 正文格式：把 validation branch、commit、fork CI run 从代码块改为 Markdown 超链接列表；同时同步更新 `internship-reports/day9-community-issue-pr-ci-watch.md` 中的 issue 草稿和 PR 草稿验证段，避免后续 PR body 继续使用代码块链接。
- 2026-07-06：按用户确认发布 estimator / FlinkDeployment / ResourceQuota e2e flake upstream issue：[karmada-io/karmada#7719](https://github.com/karmada-io/karmada/issues/7719)，标题 `FlinkDeployment e2e cleanup can leave stale APIEnablements`，正文使用 Day 9 英文草稿，包含 #7697 失败 CI、scheduler missing API 日志、本地 diagnostic、候选修复 commit `f2e7c434b` 和 fork CI `28774018375` 全绿证据。第一次尝试 `gh issue create --label kind/flake` 因权限未加上 label，后续已按 Karmada/Prow 项目格式在 issue 下评论 `/kind flake`，当前 `kind/flake` label 已生效。Day 9 的 PR 草稿已改为 `Fixes #7719`。
- 2026-07-06：按用户要求先拟定 estimator / FlinkDeployment / ResourceQuota e2e flake 的 upstream issue。已读取 Karmada `flaking-test` issue 模板，检索 open/history issue 未发现直接重复项；在 `internship-reports/day9-community-issue-pr-ci-watch.md` 中新增“独立 issue 草稿”，标题为 `FlinkDeployment e2e cleanup can leave stale APIEnablements`，正文包含 #7697 失败 job、scheduler missing API 日志、本地 diagnostic 复现旧 cleanup 边界、与 #7692/#7551/#7550 的关系、以及验证分支 `test/estimator-flink-crd-flake` commit `f2e7c434b` / fork CI `28774018375` 全绿证据。未发布 upstream issue。
- 2026-07-06：完成 estimator / FlinkDeployment / ResourceQuota e2e flake 原型验证、分类和本地 diagnostic 复现。原始 #7697 失败应归类为 e2e timing flake 现象：同一业务代码 rerun 后通过，且失败路径不在 #7697 diff 内。独立 worktree `/tmp/karmada-estimator-flake` 分支 `test/estimator-flink-crd-flake` 第一版提交 `c4e514e171` 把 `WaitCRDDisappearedOnClusters` 放在 `ClusterPropagationPolicy` cleanup 后，fork CI run `28766135233` 失败：e2e v1.34/v1.35/v1.36 都在 `ScheduleMultiTemplate / FlinkDeployment scheduling` cleanup 等 member CRD 消失超时。修正版 `78d99e024` 只等 control/member CRD 消失，fork CI run `28767618548` 全绿。本地 diagnostic 使用 `/tmp/karmada-cert-rotate-multinode-observe/data/karmada/rotated-karmada.config` 复现旧 cleanup 边界：control plane CRD 1s 后消失，但 after 0s member CRD/APIEnablements 仍存在，after 2-3s member CRD 已消失而 APIEnablements 仍显示 FlinkDeployment，after 4s 才完全收敛。基于该证据，当前候选提交升级为 `f2e7c434bad6d4a970265af79a157afb61e6182e`：新增 `WaitCRDDisappearedFromClusterStatus`，三个 FlinkDeployment CRD cleanup 同时等待 control CRD、member CRD object、`Cluster.Status.APIEnablements` 收敛；本地 `git diff --check` 和 `go test ./test/e2e/suites/base -run '^$' -count=0` 通过，fork CI run `28774018375` 全绿（Chart/CLI/Operator、lint/codegen/compile/unit、e2e v1.34/v1.35/v1.36）。未开 upstream PR，未发 upstream 评论。
- 2026-07-06：用户指出 #7697 同一提交 rerun 通过说明 CI/e2e 确实存在偶发性失败问题，后续可独立跟进。已在 Day 9 追加“后续可跟进的 e2e 稳定性问题”，并在 TODO 新增 P1 任务：围绕 estimator / FlinkDeployment / ResourceQuota timeout 先检索已有 flake issue，再决定补证据、开独立 issue 或尝试最小 e2e wait 修复。注意不要把这个 flake 修复混进 #7697 当前证书轮换 PR。
- 2026-07-06：澄清 #7692 与 #7697 失败路径的关系：#7692 确实是在做 e2e flake 修复，但覆盖的是 `test/e2e/suites/base/clusterresourcebinding_test.go` 里 cleanup 前未等 member cluster `ClusterRole` present 的问题；#7697 这次失败是在 `test/e2e/suites/base/estimator_test.go` 的 FlinkDeployment / ResourceQuota / estimator assumption 流程。#7692 可作为同类异步等待修复模板，不等于 estimator flake 已有修复 PR。
- 2026-07-06：确认 upstream PR #7697 空提交 `93eaf7e57515c959fe30fa2aba387ce10029046d` 的新一轮 CI 已跑完并全绿：DCO、lint、codegen、compile、unit test、e2e v1.34.0/v1.35.0/v1.36.1，以及 Chart/CLI/Operator 三套 Kubernetes 矩阵均通过。此前失败的 `e2e test (v1.34.0)` 没有复现，当前证据支持将原 FlinkDeployment / ResourceQuota timeout 归类为 e2e timing flake，不需要为它修改 #7697 代码。剩余门禁是 `tide` pending：缺 `approved` / `lgtm` 标签，需要 maintainer review；Codecov patch coverage warning 是非阻塞提示。最终结果已补入 `internship-reports/day9-community-issue-pr-ci-watch.md`。
- 2026-07-06：按用户要求深读 #7692 和 #7663，结论已补入 `internship-reports/day9-community-issue-pr-ci-watch.md`。#7692 是最小 e2e flake 修复：`creates work with permanent ID label` 原来只等 control plane `Work` 创建，cleanup 却等待 member cluster `ClusterRole` 消失；PR 增加 `WaitClusterRolePresentOnClustersFitWith`，确保资源经历 present -> cleanup -> disappear。#7663 是运行态 bearer token reload：移除 `rest.Config.BearerToken` 静态 token，新增 `tokenRefreshingRoundTripper` 从 `Cluster.Spec.SecretRef` Secret 按 30s TTL 重新读 token，在请求前注入 Authorization；它只覆盖 push-mode member cluster bearer token，不覆盖 X.509 client/server cert、CAData、pull-mode agent 或无需重连的既有 watch。已在临时 worktree `/tmp/karmada-pr7663` 跑 `go test ./pkg/util -run 'Test(TokenRefreshingRoundTripper|BuildClusterConfig_LongLivedClientPicksUpRotatedToken)' -count=1`，结果通过。这个深读支撑 #7697 的能力边界：rotate 更新凭据数据，不等于运行中组件自动加载新凭据；运行态 reload 需要像 #7663 一样在具体 client/transport 路径单独实现和测试。
- 2026-07-06：按用户要求开始 Day 9 社区 issue / PR 扫描，同时等待 #7697 空提交触发的新 CI 完成。新增 `internship-reports/day9-community-issue-pr-ci-watch.md` 并加入 README/TODO。扫描结论：近期看似可做的 issue 多数已有认领或对应 PR，例如 #7691 -> #7692、#7688 -> #7689、#7670 -> #7671、#7695 -> #7696、#7698 -> #7699；不建议重复实现。后续最值得读的是 #7663 token rotation hot reload PR，用来补充 #7697 “轮换凭据数据”和“运行中组件加载新凭据”的能力边界；#7692 可作为 e2e timing flake 修复模式参考。#7697 最新快照：DCO/lint/codegen/compile/unit 已通过，Chart/CLI/Operator v1.34/v1.35/v1.36 已通过，仅三个 e2e job 仍 pending。
- 2026-07-06：用户确认采用空提交方案触发 PR #7697 新一轮 CI。已在 `/home/karmada` 的 `feature/cert-mode-rotate` worktree 上创建 signed-off 空提交 `93eaf7e57515 test: trigger ci`，并推送到 `origin/feature/cert-mode-rotate`。PR #7697 head 已更新为 `93eaf7e57515c959fe30fa2aba387ce10029046d`，upstream pull_request CI 已开始：CI Workflow `28762872757`、Chart `28762872753`、CLI `28762872770`、Operator `28762872747`；fork push CI 也已开始：CI Workflow `28762870959`、Chart `28762870940`、CLI `28762870936`、Operator `28762870928`。DCO 已通过。
- 2026-07-06：排查 upstream PR #7697 当前唯一失败 check：`e2e test (v1.34.0)`，run `28499042349` / job `84472927003` / head SHA `152ab454265ac683f55f04a166e9de9aedaad94c`。失败用例是 `test/e2e/suites/base/estimator_test.go` 的 `[EstimatorAssumption] ResourceQuota plugin assumption testing`，等待 FlinkDeployment 对应 `ResourceBinding` 变为 `Scheduled=True` 超时；scheduler 日志显示当时认为 `member1` 缺少 `flink.apache.org/v1beta1/FlinkDeployment` API。#7697 改动只在 `pkg/karmadactl/cmdinit` 和生成的 CLI flags doc，且 compile/lint/unit/codegen 与 e2e v1.35/v1.36 都通过，初步分类为 e2e timing flake / cluster API discovery 同步时序问题，建议优先 rerun 失败 job，而不是修改 #7697 代码。细节已记录到 `internship-reports/day8-after-pr7697-follow-up-pr-split.md`。
- 2026-07-01：用户确认后，已将 #7697 follow-up PR comment 发布到 `karmada-io/karmada#7697`：https://github.com/karmada-io/karmada/pull/7697#issuecomment-4853917542 。评论说明设计理念、运行态多节点 kind 验证结果，以及 rotate 与组件加载新证书之间的能力边界。已用 `thread_brief.py 7697` 和 `gh pr view --comments` 确认 comment 可见；Day 8 报告已补充发布链接。
- 2026-07-01：按用户要求继续整理 #7697 follow-up PR comment 草稿，已追加到 `internship-reports/day8-after-pr7697-follow-up-pr-split.md` 末尾。草稿为英文，可直接复制到 PR，内容覆盖设计理念、运行态多节点 kind 验证结果、以及“rotate 更新证书数据”和“重启/自动加载新证书恢复工作”的能力边界；明确 #7697 只实现 init-managed leaf certificate renewal + Secret/kubeconfig update，不承诺自动过期检测、自动 rotate、自动 rollout、热加载保证、CA rotation 或 caBundle migration。未发布 upstream comment，发布前仍需用户确认完整文本。
- 2026-07-01：按用户要求在 `internship-reports/day8-after-pr7697-follow-up-pr-split.md` 末尾追加“为什么 rotate 后还需要重启相关组件”。说明 Secret 更新与组件进程加载新证书是两个阶段；Karmada init-managed 组件通过 Secret volume / kubeconfig 文件获取证书，但 #7697 不定义统一热加载、watcher 或自动 rollout 机制；若证书已过期，必须先 rotate 更新 leaf cert Secrets，再重启 etcd/apiserver/controller-manager/scheduler/webhook 等相关组件读取新证书，单纯重启旧过期 Secret 不会恢复。
- 2026-07-01：更新实习阶段目标：在 2026 年 9 月前拿到 AgentCube Karmada 项目社区席位。目标拆解为：至少持续维护 1 条高质量 upstream feature 主线，形成可复查 issue/PR/CI/review 证据；参与 Karmada issue/PR 讨论和会议表达；把每次社区反馈转化为测试、文档或代码改进；避免只做本地学习而缺少 upstream 可见产出。
- 2026-07-01：严格按 Day 6 文件级变更清单从 `upstream/master` 新建 upstream-facing 分支 `feature/cert-mode-rotate`，实现 `karmadactl init --cert-mode=rotate` 第一版。提交 `32e553925 feat: support rotating init managed certificates` 已推送到 `origin/feature/cert-mode-rotate` 触发 fork push CI。实现范围保持在 `pkg/karmadactl/cmdinit` 和生成的 `docs/command-line-flags/karmadactl_init.md`：新增 cert mode flag/config 字段、拆分 `Complete()` / `RunInit()`、新增 rotate 独立路径读取既有 CA 并重新签发 leaf cert、update-only 同步既有 cert Secrets；未修改 `command.go`、`deployments.go`、`statefulset.go`、Helm/operator、caBundle 或 workload 模板。本地验证已通过：`go test ./pkg/karmadactl/cmdinit/... -count=1`、`go test ./pkg/karmadactl/... ./cmd/karmadactl/... ./cmd/kubectl-karmada/... -count=1`、`hack/verify-command-line-flags.sh`、`hack/verify-import-aliases.sh`、`PATH="$(go env GOPATH)/bin:$PATH" golangci-lint run ./pkg/karmadactl/cmdinit/...`、`PATH="$(go env GOPATH)/bin:$PATH" hack/verify-staticcheck.sh`、`git diff --check`。
- 2026-07-01：按用户要求把“先架构/文件级 scope 设计，再写代码”的开发模式沉淀为本地 skill `.agents/skills/design-before-code/`。该 skill 要求在非平凡代码任务前先读现有实现、产出 file scope matrix、no-change list、Mermaid current/proposed flow、函数级设计和测试矩阵；如果实现中发现 scope 扩大，必须先更新设计再继续。已通过 `quick_validate.py`。
- 2026-07-01：按用户要求继续补充 `internship-reports/day6-certificate-rotation-design-implementation.md`，在末尾新增“代码级别文件变动设计”。内容包括文件触点总览、建议变更清单、第一版明确不改的文件、`RunInit()` / `Complete()` 拆分、CA 与 leaf cert 边界、Secret 更新边界、函数级草案、测试矩阵、实现顺序和 PR body 解释。当前设计结论保持不变：第一版聚焦 `karmadactl init --cert-mode=rotate`，只更新由 init 管理的证书 Secrets，复用既有 CA，不轮转 root CA，不更新 caBundle，不改 workload 模板。
- 2026-07-01：按用户要求开始准备社区会议提案。新增 `internship-reports/day7-certificate-rotation-community-proposal.md`，把 #7693 方案整理成可上会讲的 one-pager、英文 30 秒/3 分钟 talk track、scope/non-goals、Mermaid current/proposed flow、resource touch matrix、测试计划、PR 拆分和 maintainer open questions。核心会议主张：第一版聚焦 `karmadactl init --cert-mode=rotate`，只轮转组件身份证书/leaf certificates，复用既有 CA，不轮转 root CA、不更新 caBundle、不重建 workload；会议需要确认 CLI UX、CA private key 来源、是否只打印重启指导、external etcd 缺省行为。
- 2026-06-30：按用户要求新增 `internship-reports/day6-certificate-rotation-design-implementation.md`，整理 `karmadactl init` 证书轮换方案设计与实现准备。已对齐 [website#1014](https://github.com/karmada-io/website/issues/1014)、[karmada#4787](https://github.com/karmada-io/karmada/issues/4787)、[karmada#5037](https://github.com/karmada-io/karmada/pull/5037)、[website#1016](https://github.com/karmada-io/website/pull/1016) 背景。关键实现判断：#7693 第一版应聚焦 `karmadactl init --cert-mode=rotate`，复用证书生成和 Secret 更新路径；`Complete()` 当前含安装期 NodePort 检查、Node mutation、data path 清理，rotate mode 不能原样复用；根据用户确认，第一版轮转的是组件身份证书/leaf certificates，CA/root certificates 是底层信任链基石，不轮转、不更新 caBundle。
- 2026-06-30：跟进维护者新建的 upstream issue [#7693](https://github.com/karmada-io/karmada/issues/7693)。确认此前 Day 4 / #7690 / `feature/cert-manager-layout` 的重心偏向 split Secret layout 和长期证书管理抽象，而 #7693 给出的当前落地方向是先复用安装工具已有证书生成和 Secret 写入能力，为 `karmadactl init` 增加证书轮换模式，例如 `--cert-mode=rotate`。已更新 `internship-reports/day4-certificate-layout-issue-follow-up.md` 的剩余部分，补充“理解与对齐”：rotate mode 应复用 `genCerts()`、证书文件读取、`createCertsSecrets()` / `CreateOrUpdateSecret()`，但不继续执行 workload/CRD 创建；旧 split layout prototype 暂时只作为 Secret 映射参考。同步更新 TODO，把证书主线切到 #7693。
- 2026-06-30：按用户要求为 #7643 创建可复查证据分支。使用 `/home/karmada-issue-7643` 从 `upstream/master@ffbade988` 切到 `verify/issue-7643-evidence`，新增两个 evidence tests：`pkg/resourceinterpreter/customized/declarative/luavm/issue7643_verification_test.go` 和 `pkg/resourceinterpreter/default/thirdparty/issue7643_verification_test.go`。测试不仅打印输出，也断言 `0.1 -> 100m`、默认 Flink components 保持 `"100m"`、`CalculateResourceUsage` 汇总 `"200m"`。已运行两条目标 `go test` 均通过，并提交/推送 fork 分支：`ranxi2001/karmada@verify/issue-7643-evidence`，commit `46d0ba9be test: add flink memory conversion evidence`。同步更新 Day 5，把证据分支和 commit 链接加入 upstream 评论草稿。
- 2026-06-30：按用户要求把 #7643 可直接复制的 upstream 评论草稿追加到 `internship-reports/day5-issue-7643-flink-memory-verification.md`，包含 Mermaid 对比图、函数级验证输出、默认 FlinkDeployment customization 路径输出和当前结论。同步更新 `AGENTS.md`：以后准备 upstream comment、PR 描述、review 文案、Mermaid 解释等可复用长文本时，必须写入报告或草稿文件，不要只在终端/chat 输出里交付。
- 2026-06-30：跟进 upstream issue #7643，按 maintainer 要求做函数级和运行路径验证。使用独立 worktree `/home/karmada-issue-7643` 基于 `upstream/master@ffbade988` 添加临时测试，确认 `kube.getResourceQuantity("100m")` 返回 Lua number `0.1`，但 Lua -> Go JSON 转换后是 `resource.Quantity("100m")`；默认 FlinkDeployment thirdparty interpreter 输出 JM/TM memory 均为 `"100m"`，`helper.CalculateResourceUsage()` 汇总为 `{"cpu":"300m","memory":"200m"}`。结论写入 `internship-reports/day5-issue-7643-flink-memory-verification.md`：当前 upstream master 未复现 issue 描述的 “100m 变成 1 并导致总量错误”；若要回复 upstream issue，先让用户确认英文评论文本。
- 2026-06-30：按用户要求阅读官方 [Certificate Framework](https://karmada.io/docs/administrator/security/cert-framework/) 并对照仓库内 `docs/proposals/cert/Self-Signed_Certificate_Content_Standardization.md`、`hack/deploy-karmada.sh`、`artifacts/deploy/*.yaml`。更新 Day 4：补充官方 cert framework 证书定义词表和与 prototype 的差异。关键修正：当前官方框架已在 `hack/deploy-karmada.sh` 落地，`karmadactl init`/operator/Helm 未来对齐；prototype 还没有完全对齐官方 CA 模型和组件覆盖范围，尤其是保留 `EtcdCA`、未覆盖 search/metrics-adapter/scheduler-estimator server cert/search etcd client 等，需要 PR 前重新注释和确认。
- 2026-06-30：用户已在 #7690 下评论 `/kind feature`，issue label 已生效为 `kind/feature`。新增 Day 4 记录 `internship-reports/day4-certificate-layout-issue-follow-up.md`，分析 `feature/cert-manager-layout` branch 与 #7690 提案之间的 gap：branch 覆盖第一版 `karmadactl init` plan-based split layout prototype，但不覆盖图片中的长期证书管理系统；主要待确认点是 maintainer 方向、是否拆小 PR、命名规范、RBAC/client identity 是否 follow-up、以及真实 split layout smoke test。
- 2026-06-30：按用户确认发布 upstream issue。使用 Day 3 中已确认的英文草稿新建 `karmada-io/karmada#7690`：`Proposal: plan-based split certificate Secret layout for karmadactl init`，正文包含 #6051/#6670/#6788 关联、scope/non-goals、两张设计图 raw 链接、prototype branch/commit/CI 证据，并 mention `@zhzhuang-zju` 请求 review。已用 `thread_brief.py 7690` 确认为 open，暂无 labels、assignees、milestone、comments。
- 2026-06-30：按“新建 issue 最好带图片引用解释”的要求，增强 Day 3 upstream issue 草稿：加入两张已推到 fork `intern` 分支的设计图 raw 链接，并验证链接返回 `200 image/png`。草稿中明确图片只作为长期方向和前后对比说明，当前 issue/PR scope 仍限于 `karmadactl init` 的 plan-based certificate Secret layout，不引入 CRD/controller/cert-manager 集成。
- 2026-06-30：按“先做成 issue、让 @zhzhuang-zju review 提案”的社区流程要求，重新核对 #6051、#6670、#6788：#6051 是证书/配置命名规范 umbrella，#6670 是证书标准化 proposal，#6788 是已有 open split secret layout PR。已在 Day 3 增加英文 upstream issue 草稿，明确该提案与三者的关系、plan-based 抽象边界、prototype branch、CI 证据和待 maintainer 确认的问题；未发布 upstream issue/comment，也未自动 mention maintainer，等待用户确认目标和完整文本。
- 2026-06-30：确认 `feature/cert-manager-layout` commit `eb02bde96` fork push CI 全绿：18 个 check runs 中 16 success、2 skipped、0 failed。新增 Day 3 的 PR 审阅准备部分，整理当前 PR 的实现边界、未实现内容、文件修改解释、reviewer 重点关注点、本地验证命令、fork CI 结果和 upstream PR 文案草稿；补充两张方案图 `day3-karmada-certificate-management-proposal-data-flow.png`、`day3-karmada-certificate-management-comparison-data-flow.png` 作为长期方案/对比说明资料。
- 2026-06-30：分析 fork push CI 结果。`feature/cert-manager-layout` commit `651cbec29` 的 push CI 已全部结束，只有 `CI Workflow / lint` 失败；compile、unit test、codegen、3 个 e2e、CLI / Chart / Operator Kubernetes 矩阵均通过或 skipped。失败根因是新增 `pkg/karmadactl/cmdinit/certmanager` 包未满足 `.golangci.yml` 静态检查：导出符号缺 Go doc、Secret 名称常量触发 gosec G101 误报、测试循环可改 `slices.Contains`、`legacyCertificateNames` 参数未使用。已把复盘和提交前 lint checklist 写入 Day 3，并在 TODO 加入 P0 lint 习惯项。
- 2026-06-29：完成 Day 3 证书管理方向任务整理。新增 `internship-reports/day3-certificate-management-task-triage.md`，梳理 community#69、#6091、#6269、#6670、#6788、#6553 和 #6051 的关系；结论是优先跟进 #6051 中 Helm 证书 Secret / volume / mount path naming convention 的 `help wanted` 空位。随后按“不要为了改代码而改代码，先抽象证书管理层”的反馈，追加了批量系统证书替换分发的初步设计，包括证书身份、layout plan、Secret plan、组件 kubeconfig 分发和验证计划，并加入 Mermaid 前后对比图说明抽象层引入后的变化。同步更新 `internship-reports/README.md` 和 `internship-reports/todo.md`。
- 2026-06-29：按用户反馈修整 `internship-reports/day2-karmada-architecture.drawio`。改为真正用 draw.io CLI 导出并视觉检查，入口流程调整为 User -> Karmada API Server，Admission Webhook 画成 API Server 的 admission 调用；移除长边标签，用编号 1-6 + 底部图例表达流程；重新导出 `day2-karmada-architecture.png`、`day2-karmada-architecture.drawio.png`、`day2-karmada-architecture.drawio.svg`，`validate.py` 结果为 `0 error(s), 0 warning(s)`。当前 Day 2 相关文件已暂存，尚未 commit/push，等待用户确认图。
- 2026-06-29：完成 Day 2 Karmada 项目理解。新增 `internship-reports/day2-karmada-project-understanding.md`，面向首次接触 Karmada、仅有基础 K8s 概念的读者，用 Mermaid 梳理控制面、ResourceTemplate -> PropagationPolicy -> ResourceBinding -> Work -> member cluster 传播链路、nginx 样例和源码目录地图；新增 `internship-reports/day2-karmada-architecture.drawio` 作为可编辑架构图源文件，并导出 `internship-reports/day2-karmada-architecture.png`、`internship-reports/day2-karmada-architecture.drawio.png`、`internship-reports/day2-karmada-architecture.drawio.svg`。
- 2026-06-28：处理 GitHub fork sync 提示。`origin/master` 曾比 `upstream/master` 多 1 个 personal fork PR #1 merge commit `410202123`，同时落后 upstream 3 个 commit；已按 fork `master` 镜像规则把本地/远端 `master` force-with-lease 重置到 `upstream/master` `56d5d87ec`。当前 `origin/master` 与 `upstream/master` 一致，`intern` 保留实习记录不受影响。
- 2026-06-28：按 mentor 反馈同步修正 Karmada CI 验证规则：以后不再向个人 fork 仓库提 PR 来跑 CI。Karmada 与 AgentCube 不同，`.github/workflows/ci.yml` 已经明确支持 `push` 到 fork 分支触发 CI（排除 `dependabot/**`），所以预提交 upstream 前直接 push topic/validation branch 到 `origin` 并查看 commit SHA Actions/checks；如失败先分类为代码问题、fork 环境差异、缺少 tag/history、CI flake 或 upstream-only gate。已更新 `AGENTS.md` 和 `.agents/skills/karmada-pr-management/SKILL.md`。
- 2026-06-26：从 `master` 新建并切换到本地 `intern` 分支。
- 2026-06-26：参考 AgentCube 实习记录结构，开始为 Karmada 建立基础文件：根目录 `AGENTS.md`、`PROGRESS.md`、`internship-reports/README.md`、`internship-reports/todo.md`、`internship-reports/intern-glossary.md`，以及本地可复用 skill `.agents/skills/open-source-onboarding/`。
- 2026-06-26：从 AgentCube 迁移 3 个本地 skills 到 Karmada：`.agents/skills/drawio-skill/` 保留通用绘图能力；`agentcube-pr-management` 改造为 `.agents/skills/karmada-pr-management/`，默认 repo 为 `karmada-io/karmada`、基线分支为 `upstream/master`、测试命令按 Karmada `make test` / `make verify` / `hack/update-*`；`agentcube-issue-discussion` 改造为 `.agents/skills/karmada-issue-discussion/`，脚本默认 repo 改为 `karmada-io/karmada`。4 个本地 skills 已通过 `quick_validate.py`。
- 2026-06-26：配置 `upstream=https://github.com/karmada-io/karmada.git`，从 `upstream/master` 创建 topic branch `test/update-default-control-plane-images`，完成 #7598 follow-up：同步 Helm chart、`karmadactl init`、`karmada-operator`、raw deploy manifest 和 `hack/deploy-karmada.sh` 的默认 Kubernetes / etcd 版本，并提交 upstream PR #7666。

## Current Blockers

- #7662 已有本地 contributor target-first line review，但尚无 maintainer 技术结论；proposal 未确定 GracefulEviction relationship、迁移事务持久化、并发 ownership 和旧 API default/TTL 合同。在这些边界确认前，不进入 SafeMigration Go 实现。
- #7697 current head `bf24e47ce` 已关闭 SAN reconstruction、shared-CA local identity、external-etcd replacement 和 config operation contract 四个源码/方案 blocker，PR body 与 author comments 已同步。attempt 1 的 v1.35/v1.36 均与证书 diff 无关，upstream attempt 2 和 fork same-SHA validation 全绿，current head 17/17 checks success；剩余门禁是 bot thread 清理和真人 review。
- 尚未实际运行完整 `hack/local-up-karmada.sh` 多集群路径；但 Docker、kind、隔离 kubeconfig、Karmada init/rotate 和双节点 host control plane 已通过 #7697 真实过期实验验证。
- 尚未跑完整 `make test` 或 `make verify`。本次 PR 只跑了相关范围的 `go test`、`hack/verify-command-line-flags.sh`、Helm lint、脚本语法和 YAML 解析。
- upstream PR #7732 已由 `@RainbowMango` `/lgtm`、`/approve` 并合并，不再是 blocker。
- GitHub CLI 当前已通过 `GH_TOKEN` 登录 `ranxi2001`，repo/API 查询可用；token 缺少 `read:org`，但不影响本次公开仓库 issue/PR 扫描。
- 当前 Windows 电脑上 draw.io 已安装但未加入 PATH：`drawio` / `draw.io` 命令不可用，系统级路径 `C:\Program Files\draw.io\draw.io.exe` 也不存在；实际可用路径是 `C:\Users\ranxi\AppData\Local\Programs\draw.io\draw.io.exe`，版本 `30.2.6`。这个路径只适用于当前 Windows 机器；macOS 或其他机器仍按 drawio-skill 的正常探测顺序处理。

## Ruled Out

- 不改写根 `README.md` 作为实习导航，避免污染上游项目介绍。
- 不把实习报告、中文学习笔记、本地 benchmark 原始数据或本地 skills 放到 upstream-facing topic branch。

## Next

- #7623：blocking line review 已发布；等待作者回复或更新 commit。后续只检查 cache commit 是否移到完整 executor rebuild/status reconciliation 成功之后，以及是否新增 status failure retry 的 controller regression；不要复述 Copilot 的 false data-race finding，也不要把已有 map cleanup gap 混入当前 blocker。
- #7764：当前 head `7570842fb5` 已修 member layout 并合并普通段落 hard-wrap；compatibility artifact scope、fast-wait stale inference、single-hit no-retry inference 和 flat glob 仍待处理。Artifact thread open/current，fast-wait resolved/outdated但原文未变，retry open/outdated，hard-wrap open/outdated但代码已采纳。后续回复除 exact-text approval 外还必须通过 standalone comprehension 与 visualization gates；已有 fast-wait/retry Mermaid，尚未发布。
- #7621 / #7662：PreserveReady behavior matrix、SafeMigration scheduler collision、direct-delete falsification、2026-06-16 目标片段和 2026-06-30 全量会议证据均已完成；target-first 评论 A 已发布。下一步按 Day 23 的七个问题对齐 Story 2 supported modes/Descheduler 分工，以及 Story 3 target authority/rolling owner/durable state/cancel 合同；候选 B direct-deletion/finalizer 发布前仍需用户确认 exact target/text，之后再请求可认领的独立 test/design slice。
- #7732 已合并并归档；把 #7719 的 source-backed causal timeline 作为后续 flake review 模板，不再跟踪该 PR 的 checks/review。
- #7757 已由 `@Priyanshu8023` 认领；复现/review 评论已发布并 `cc @RainbowMango`。下一步等待维护者确认语义和作者 PR，后续只协助 review，不重复实现。
- #7692 无阻塞性 finding 的 comment review 已发布；等待作者或 test OWNERS 回复，不重复催促。之后可 review #7754，复用 Day 5 证据明确当前 Quantity 语义本来正确、改动价值是去掉不必要 float round trip。
- 以“9 月前拿到 AgentCube Karmada 项目社区席位”为主目标管理后续工作：每周至少沉淀 1 个 upstream-visible 证据，包括 merged PR、reviewed PR、issue 验证、社区会议材料、maintainer feedback follow-up 或可复用测试/文档改进。
- #7697 源码和方案状态为 `CODE_REVIEW_READY`：代码/review 入口为 Day 26，E2E RCA 与新窗口统计入口为 Day 27。CI 已全绿；下一步清理已处理 bot threads 并请求 human review。所有 upstream 动作仍需分别准备 exact text/action 后确认。
- estimator / FlinkDeployment / ResourceQuota e2e flake 已闭环：issue #7719 closed，修复 PR [#7732](https://github.com/karmada-io/karmada/pull/7732) merged as `d0714678`。后续只复用 Day 11 RCA 方法，不再追加评论或改动。
- CI flake 专项下一步：Remedy `3861906f2` 与 migration `8ef8a08c6` 已完成独立 fork branch、最小 diff、preflight 和三版本 E2E 全绿。下一步为两项分别准备 concise issue/PR exact target/text，先让用户确认再发布；其他 25 个 flake jobs 继续补 E3 RCA，不开 timeout/retry 型 PR。
- #7693 由 open PR #7697 实现中；第一版限定 CLI-only leaf renewal、复用 init-managed CA、preserve-only external-etcd credentials、update-only Secret 同步、不更新 `caBundle`、不自动重启、不改 Helm/operator。代码范围已收敛，不拉入 #7690 split layout 或完整证书标准化；发布前网站手册由 #1014 协调补齐。
- #7690 / `feature/cert-manager-layout` 暂时作为背景和 Secret 映射参考，不作为当前第一优先级 PR。旧 branch 已通过 fork push CI，但不要直接从它开 upstream PR。
- 如需跟进 #7643 upstream，先发送 Day 5 中的英文 verification comment draft 给用户确认；当前不建议开重复 PR，因为 issue 已有 assignee，且验证结果不支持 functional bug 结论。
- 继续观察 upstream PR #7666 的 CI 和 review；如果失败，先区分代码问题、环境抖动和 CI 环境差异。
- 阅读 Karmada 官方入口：`README.md`、`CONTRIBUTING.md`、`docs/README.md`、`docs/images/architecture.png`、`samples/nginx/README.md`。
- 补 Quick Start 预检报告：跑通或拆解 `hack/local-up-karmada.sh`，记录环境、失败命令、错误、绕过方式和最终结果。
- Day 3 深追 `samples/nginx` 传播链路：从 `Deployment` + `PropagationPolicy` 到 `ResourceBinding`、`Work` 和 member cluster manifest，重点读 `pkg/detector/`、`pkg/controllers/binding/`、`pkg/controllers/execution/`。
- 证书方向下一步：先把证书管理层设计收敛成更小的代码边界，避免在部署代码里散落 `split` 判断；再根据 mentor 方向决定是继续 `karmadactl init` split layout，还是回到 #6051 Helm 证书 Secret / volume / mount path 差距表。
- 如果要找第一个贡献点，先从 docs、tests、good first issue、flaking test、CLI help 或小型 validation/test gap 入手。
- 需要绘制 Karmada 数据/事件/控制流或时序图时，使用 `.agents/skills/project-mermaid/` 维护 canonical `.mmd` 和生成的 PNG/SVG；需要跨层架构、拓扑、vendor icons、swimlane 或精细自定义布局时，使用 `.agents/skills/drawio-skill/`。
- 分析 Karmada issue/PR 或准备 upstream PR 时，优先使用 `.agents/skills/karmada-issue-discussion/` 和 `.agents/skills/karmada-pr-management/`。

## Stop Conditions

- #7662 未形成 maintainer design direction，或作者正在修改同一 API/controller 时，不开重复实现 PR；只有 bot 建议和 green docs CI 不能授权实现。
- #7697 持续维护任务只在 PR 已 merge、merge SHA/最终 CI/review 状态已归档、deferred follow-ups 已记录后才标记 `DONE`。如 PR 只是 closed 但未 merge，记录 maintainer 原因并标记 `BLOCKED` 或转入新 PR，不视为完成。
- 同一个本地环境问题连续失败 3 次，例如 kind 集群创建、Docker 镜像拉取、证书生成或 kubeconfig 上下文混乱，就停止硬调，记录 BLOCKED 并换路径。
- 如果某个 issue 已有人明确认领并正在修改同一问题，不开重复 PR，只做复现、测试、review 或文档补充。
- 如果只能基于猜测发社区评论，先停止，补源码证据、官方文档引用或本地验证结果后再讨论。
