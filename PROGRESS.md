# PROGRESS.md

这个文件是给 Agent 工作循环用的短记忆，不是日报。每次开始工作先读这里，每次结束只更新关键状态，避免下一轮从零开始。

## Goal

阶段目标：在 2026 年 9 月前拿到 AgentCube Karmada 项目社区席位。

当前主线：围绕 Karmada upstream 真实贡献建立稳定产出能力，不再停留在学习仓库搭建。重点是持续完成可 review、可合并、可复盘的社区工作：高质量 issue/PR triage、设计提案、函数级验证、测试补强、PR review 响应和社区会议表达。短期以 `karmadactl init` 证书轮换 PR 为主线，沉淀一套可复用的 Karmada 贡献流程，形成 mentor 可检查的社区影响力证据。

## Last Run

- 2026-07-10：按用户要求重构 `internship-reports/day11-ci-flake-statistics.md`，把“一页结论 / 最新关注 / 高置信 Flake / 持续关注台账”放在正文前半部，将统计口径、rerun/trigger 样本、PR #7732 文案与技术复查、待补日志、排除项和数据采集命令下沉到附录。最新 GitHub 快照显示 #7732 仍 open、无 human review，核心 lint/codegen/compile/unit/e2e v1.34-v1.36 通过；此前 cancelled 的 Chart/Operator workflows 已进入 attempt 2，当前各有一个 job pending，Tide 仍等待 `approved`/`lgtm`。TODO 已同步改为等待 rerun 收尾与 human review。
- 2026-07-10：继续清理遗漏的 #7719 issue body 临时副本。第二次 FlinkDeployment timeout、#7697/#7728 空提交与 `files=0` 证据已在 Day 11 归档，Day 9 已补最终 issue 更新索引，因此删除独立临时 body 文件并继续使用 Day 9/Day 11 作为本地入口。
- 2026-07-10：按用户要求整理 `internship-reports/` 下以 `pr` 开头的 8 个临时文件。#7697 最终 scope comment 合并回 Day 8；#7719/#7732 的设计、最终验证和代码复查合并回 Day 11，Day 9 删除过期 PR body 草稿并改为结果索引；#7728/dashboard/website 的最终 body、验证和 PR 状态统一保留在 Day 10，删除重复旧草稿；README/TODO/PROGRESS 均改为 Day 文档入口。整理后不再保留独立 `pr*` body/plan/review 文件。验证时本机没有 `ruby`，改用 Perl 完成 Markdown fence 检查；文件名扫描、删除文件引用扫描、本地 Markdown 链接检查和 `git diff --check` 均通过。
- 2026-07-10：完整复查 upstream PR #7732 当前 head `1240559dd`。读取 PR/issue body、全部 issue/line review/review、4 文件 diff、相关 helper/APIEnablement 实现、Ginkgo 并行/Serial 配置和最新 checks；确认仓库内 4 个 FlinkDeployment CRD 创建路径均覆盖 control plane CRD、member CRD、Cluster.Status.APIEnablements 三段 cleanup barrier，DeferCleanup LIFO 顺序正确，`APIUnknown` 不会让下一轮 setup 误判 ready，未发现 correctness finding。重新执行 `git diff --check upstream/master...upstream/pr-7732` 和 `go test ./test/e2e/framework ./test/e2e/suites/base -run '^$' -count=0` 均通过，topic worktree 干净且 Signed-off-by 存在。upstream lint/codegen/compile/unit/e2e v1.34-v1.36 通过；Chart v1.34 与 Operator v1.35 各一个 job 仅标记 `The operation was canceled.`，同矩阵其他版本和 fork workflows 通过，归类为 CI/process interruption，不是代码 finding。评审结论已合并到 `internship-reports/day11-ci-flake-statistics.md`，下一步等待 human `lgtm`/approval；未执行任何 upstream-facing action。
- 2026-07-10：用户确认后已在 upstream PR #7732 的 Gemini line review thread 回复，链接 https://github.com/karmada-io/karmada/pull/7732#discussion_r3555941597 。回复说明已核对 Gomega `Eventually(func(g gomega.Gomega) ...)` 语义和 Karmada 现有 e2e helper 风格，`g.Expect(err).NotTo(gomega.HaveOccurred())` 失败会停止当前 poll 并让 Eventually 重试，不会继续执行到 nil `cluster.APIEnablement(...)`；除非 maintainer 更偏好显式返回 error，否则保持当前风格。
- 2026-07-10：验证 PR #7732 中 Gemini line comment 是否为真实 bug。使用临时测试 `test/e2e/framework/gomega_eventually_temporary_test.go` 复现 `Eventually(func(g gomega.Gomega) ...)` 控制流：当 `g.Expect(err).NotTo(HaveOccurred())` 失败时，测试输出 `continuedAfterFailedAssertion=false`，失败停在 assertion 行，没有执行到后续 nil dereference；另验证返回 `(false, err)` 的建议写法可以正常重试并通过。结论：Gemini 评论中的 nil pointer panic 判断不成立；返回 error 只是更显式的风格改进，不是必须修复的真实 bug。临时测试文件已删除，`/tmp/karmada-7719-flink-flake` 工作区干净；该复盘已补入 `code-review-growth` pattern library。
- 2026-07-09：用户确认后已创建 upstream PR [karmada-io/karmada#7732](https://github.com/karmada-io/karmada/pull/7732)，标题 `test(e2e): wait for FlinkDeployment CRD cleanup`，目标 `karmada-io/karmada:master <- ranxi2001:test/flinkdeployment-crd-cleanup`。PR 为 open、非 draft，base/head、正文和 commit `1240559dd34cc0eedd0ec6cffe97b5c0076660dc` 均核对正确；DCO 已通过，`kind/flake` label 已生效，tide pending 需要 approved/lgtm，CI Workflow / Chart / CLI / Operator 初始 checks 正在 queued/pending。
- 2026-07-09：为 #7719 FlinkDeployment e2e flake 修复做 upstream PR 最终 preflight。`/tmp/karmada-7719-flink-flake` 当前在 `test/flinkdeployment-crd-cleanup`，本地 HEAD 与 `origin/test/flinkdeployment-crd-cleanup` 均为 `1240559dd34cc0eedd0ec6cffe97b5c0076660dc`，并且 `upstream/master` 是 HEAD 祖先。重新核对 `git diff --check upstream/master...HEAD`、`go test ./test/e2e/framework ./test/e2e/suites/base -run '^$' -count=0` 均通过；open PR 搜索未发现 #7719 重复 PR；fork push Actions 为 CI Workflow / Chart / CLI / Operator success，FOSSA / image-scanning skipped。下一步等待用户确认 exact target/title/body 后创建 upstream PR。
- 2026-07-09：按用户反馈重新梳理 `internship-reports/day11-ci-flake-statistics.md`，把前半部分从冗长统计口径改为“目标与结论 / 简化口径 / 关键数字 / Rerun-Trigger 证据”，将 GitHub 命令和中间文件下沉到附录。整理后 Day 11 开头直接给出：598 条 upstream run、32 条 failed run、56 行 e2e/setup/Kubernetes test 非成功 job、3 类高置信 flake、4 个 upstream 高置信样本、1 个 fork CI 补充样本。期间 `test/flinkdeployment-crd-cleanup` fork CI run `29006012630` attempt 2 完成，失败的 v1.34 job `86098566248` 已通过；最终 fork push CI 为 `2 skipped, 4 success`。#7719 的设计和 PR 验证结果现统一归档在 Day 11 与 TODO。
- 2026-07-09：用户询问当前 `test/flinkdeployment-crd-cleanup` v1.34 fork CI 控制面失稳问题是否已记录到 Day 11。已确认 Day 11 原本已有 “Aggregated API / control plane 短暂不可用导致多用例连锁失败” 类别，但未包含这次个人 fork push CI 样本。已把 `run 29006012630 / job 86080188511` 作为“不计入 upstream 598 条 run 总量统计”的补充样本追加到 `internship-reports/day11-ci-flake-statistics.md`，记录 `etcdserver: request timed out`、API `connection refused`、`leader election lost`、多控制面容器退出以及 FlinkDeployment cleanup 属后续连锁超时。
- 2026-07-09：排查 `test/flinkdeployment-crd-cleanup` commit `1240559dd34cc0eedd0ec6cffe97b5c0076660dc` 的 fork push CI 失败。Chart / CLI / Operator 均通过；CI Workflow 中 lint、codegen、compile、unit test、e2e v1.35.0、e2e v1.36.1 均通过，只有 e2e v1.34.0 job `86080188511` 失败。失败时间线显示最早硬失败是 `09:26:51` 的 `[Suspension] ClusterPropagationPolicy testing [It] suspend the CPP dispatching`，`UpdateClusterPropagationPolicyWithSpec` 返回 `etcdserver: request timed out`；此前 artifact 中 `09:26:12-09:26:19` 已有多个控制面容器退出，`karmada-controller-manager` 记录 `leader election lost`，后续大量 Karmada/member API `connection refused`。`ScheduleMultiTemplate` FlinkDeployment cleanup 在新增 member CRD wait 处超时发生在控制面失稳之后。当前判断为 v1.34 e2e 环境整体 flake，不是 #7719 修复代码的确定性失败。已执行 `gh run rerun --repo ranxi2001/karmada --job 86080188511`，run `29006012630` 进入 attempt 2，新的 e2e v1.34 job 为 `86098566248`；后续 attempt 2 已通过，最终 fork push CI 全绿。详细证据现统一归档在 Day 11 与 TODO。
- 2026-07-09：开始准备 #7719 FlinkDeployment e2e flake 修复 PR。按 design-before-code 流程确定 4 文件 scope，从最新 `upstream/master@3d4d14d74` 创建干净 worktree `/tmp/karmada-7719-flink-flake` 和分支 `test/flinkdeployment-crd-cleanup`，cherry-pick 候选修复为 commit `1240559dd34cc0eedd0ec6cffe97b5c0076660dc`（标题 `test(e2e): wait for FlinkDeployment CRD cleanup`）。本地验证 `git diff --check` 与 `go test ./test/e2e/framework ./test/e2e/suites/base -run '^$' -count=0` 均通过。分支已推送到 `origin/test/flinkdeployment-crd-cleanup`，fork push CI 已触发：`CI Workflow` run `29006012630`、`Chart` `29006012662`、`CLI` run `29006012583`、`Operator` run `29006012840`；设计和最终 PR 状态现统一归档在 Day 11。
- 2026-07-09：用户确认后已更新 upstream issue `karmada-io/karmada#7719` body，GitHub 返回 https://github.com/karmada-io/karmada/issues/7719，`updatedAt=2026-07-09T08:37:26Z`。新正文保留原有 FlinkDeployment cleanup/APIEnablements race 分析，并补充两条新证据：#7728 merge 后 master push run `28998390044` / job `86054168911` 在 `EstimatorAssumption NodeResource` FlinkDeployment 用例中同样因 `ResourceBinding` not found / `resourcebinding.go:47` timeout 失败；#7697 的 `93eaf7e` 与 #7728 的 `de3b6be` 均是 `test: trigger ci` 空提交，commit stats 为 `files=0/additions=0/deletions=0`，后续 PR CI 全绿。已用 `gh issue view` 验证正文包含 `28998390044`、`86054168911`、两个 trigger commit SHA 和 `files=0`；最终证据现归档在 Day 9/Day 11。
- 2026-07-09：补充 Day 11 CI flake 专项里的 “rerun / trigger CI 转绿统计”。Actions REST API 显示统计窗口内 598 条 run 中有 10 条 `run_attempt > 1`：`failure -> success` 2 条（`run 28212061472` CLI workflow 的 v1.35/v1.36 Kubernetes test、`run 28256528352` CI Workflow 的 v1.36 e2e，第二次 attempt 均无失败 job）、`action_required -> success` 4 条、release `failure -> failure` 3 条、`failure -> cancelled` 1 条。另确认空提交 trigger CI 高置信样本 2 个：#7697 的 `93eaf7e` 和 #7728 的 `de3b6be` 均为 `test: trigger ci` 空提交，GitHub commit API 显示 `files=0/additions=0/deletions=0`，后续 PR CI Workflow 及 Chart/CLI/Operator matrix 直接全绿。
- 2026-07-09：新增 `internship-reports/day11-ci-flake-statistics.md`，完成 mentor 要求的 CI flake 专项初版。统计窗口为 `2026-06-26` 到 `2026-07-09`，覆盖 598 条 GitHub Actions run，其中 failure 32 条；展开后非成功 job rows 72 条，e2e/setup/Kubernetes test 类 56 条。高置信 flake 分三类：FlinkDeployment estimator ResourceBinding timeout（关联 open issue `#7719`，也命中 #7697 和 #7728 merge 后 master push）、Remedy `TrafficControl` cleanup timeout（关联历史 closed issue `#5323`）、aggregated API / etcd transient 503（#7728 PR CI v1.35，空提交 trigger 后全绿）。已把 #6841/#7388/#7719/#7691/#7692/#5323/#3667/#3682/#3699/#5263/#4427/#7697/#7728 作为相关 PR/Issue 台账整理进报告，并更新 `internship-reports/README.md` 和 `internship-reports/todo.md`。
- 2026-07-09：排查已合并 PR `karmada-io/karmada#7728` 的 master push CI 失败。merge commit `3d4d14d746de507164abf40c1017b1f2b0e47e3a` 的 `CI Workflow` run `28998390044` 只有两个 e2e matrix job 失败：`e2e test (v1.36.1)` job `86054168903` 失败在 Remedy `test with nil decision matches remedy / Create an immediately type remedy, then remove it`，删除 Remedy 后等待 `Cluster.Status.RemedyActions` 清掉 `TrafficControl` 超时，关联历史 flake `#5323`；`e2e test (v1.34.0)` job `86054168911` 失败在 FlinkDeployment estimator assumption 流程，等待 ResourceBinding 出现/进入调度结果超时，关联已 open 的 flake issue `#7719`。同一 run 的 lint/codegen/compile/unit/e2e v1.35 通过，其他 merge-triggered workflows 也通过；PR head `de3b6be675` 的 upstream PR CI 和 fork push CI 均全绿。当前判断是合并后 master CI 命中两个 e2e flake，不是 Ubuntu 24.04 runner 升级的确定性失败；建议先重跑 failed jobs。详细证据已补入 `internship-reports/day10-ci-ubuntu-runner-upgrade.md`。
- 2026-07-09：根据报告图片重命名后的链接变化，先尝试把 upstream PR `karmada-io/karmada#7697` 的既有评论 `#issuecomment-4851795706` 图片 raw URL 改为新文件名；随后确认 GitHub 评论不适合依赖 fork 内 raw 图片链接，用户已在网页评论框直接上传图片。GitHub API 验证该评论当前使用 `https://github.com/user-attachments/assets/2b9c0efb-2fad-4f93-b05e-df9e4cdfeba1`，`updated_at=2026-07-09T01:17:31Z`。最终发布文本现归档在 Day 8。
- 2026-07-08：整理 `internship-reports/` 图片文件命名，把 6 张无 day 前缀图片重命名为 `day2-...`、`day3-...`、`day8-...` 格式，并同步更新 Day 3 / Day 8 报告里的本地 Markdown 链接和 raw GitHub 图片 URL。已补充 `AGENTS.md` 规则：报告图片和导出视觉资产必须使用匹配报告的 `dayN-` 前缀。
- 2026-07-08：复查 `volcano-sh/agentcube#400` PicoD metrics PR 当前 head `1f0af1c8158735ae26b9debdf752f6277ad3ab65`。本地 clone `/tmp/agentcube-pr400`，`go test ./pkg/picod -count=1`、`go test -race ./pkg/picod -count=1`、`go test ./pkg/picod -run 'TestMetrics|TestServer_MaxBodySizeMiddleware|TestNewServer_JWTMode_RequiresAuth' -count=1` 和 `go test ./pkg/picod -run TestMetrics_Exposition -count=50` 均通过。新增临时 panic route 测试验证 maintainer 评论：当前顺序 `Logger -> Recovery -> Metrics -> maxBody -> gzip` 下，panic 被 `Recovery()` 转成 500 后不会回到 metrics 的 post-`c.Next()` 记录逻辑，因此 `GET /panic status_code=500` 缺失；临时把 metrics 放到 `Recovery()` 前后同一测试通过。另注意 `metrics_test.go` 里 `SampleSum > 0` 属低概率 flaky 断言，已补入 `.agents/skills/code-review-growth/references/review-patterns.md`。
- 2026-07-08：按用户要求把代码 review 能力提升流程沉淀为本地 skill `.agents/skills/code-review-growth/`，并通过 `quick_validate.py`。该 skill 要求 review 前读取 PR body、diff、CI、普通 comments 和 line review comments，先建立变更模型和风险面，再逐项检查正常路径、提前返回、panic/recovery、timeout/cancel、cleanup、observability 和测试覆盖；已把 AgentCube #400 的 Gin middleware metrics 漏检复盘写入 `references/review-patterns.md`，包括 metrics 必须包住 `gin.Recovery()`、body-size abort、Prometheus label cardinality 和 metric 语义边界。
- 2026-07-08：根据用户发现的 Karmada GitHub workflow Ubuntu runner 版本问题，调研上次 runner 升级 PR #3699：当时为解决 #3667 kind/e2e CI flake，将多处 workflow 从 `ubuntu-20.04` 统一升到 `ubuntu-22.04`。确认 GitHub runner-images 已公告 `ubuntu-22.04` 将于 2026-09-17 开始 deprecation、2027-04-17 unsupported；Ubuntu 24.04 LTS 标准安全维护到 2029 年 5 月，GitHub hosted runner 侧当前是 GA image。已从最新 `upstream/master@d01d3a8fd` 在独立 worktree `/tmp/karmada-ci-ubuntu24` 创建分支 `chore/update-github-runner-ubuntu-24`，提交 `0f62fd62b ci: update GitHub runners to Ubuntu 24.04`，把 `.github/workflows` 下 18 个文件共 30 处 `ubuntu-22.04` 统一替换为 `ubuntu-24.04`。本地 `git diff --check`、无残留 `ubuntu-22.04`、Python YAML parse 通过；`actionlint` 本地未安装。分支已推送到 `origin/chore/update-github-runner-ubuntu-24`，fork push CI commit `0f62fd62b05802961447601da9000403139b600d`：Chart/CLI/Operator 已 passed，CI Workflow 的 lint/codegen/compile/unit test 已 passed，主 e2e 预计耗时较长仍在 running，用户要求先不等待。已新增 `internship-reports/day10-ci-ubuntu-runner-upgrade.md`，并在用户确认标题/body 后创建 upstream PR [#7728](https://github.com/karmada-io/karmada/pull/7728)；PR DCO 已通过，upstream CI 正在运行。
- 2026-07-08：跟进 upstream PR #7728 CI，当前只有 `CI Workflow / e2e test (v1.35.0)` 失败，job `85774432012` / run `28912823833`；同一 PR 的 lint/codegen/compile/unit、e2e v1.34/v1.36、Chart/CLI/Operator 全部通过。失败表现为 `karmadactl exec` 超时、`karmadactl get` 和 `SynchronizedAfterSuite` 读 `clusters.cluster.karmada.io` 返回 `503 ServiceUnavailable`。artifact 日志显示 `02:51:14` 起 kube/karmada apiserver 出现 etcd `DeadlineExceeded` / handler timeout，两个 `karmada-aggregated-apiserver` 副本 `02:51:38` 停止 serving，`02:52:21/02:52:24` 重新启动并恢复 `cluster.karmada.io v1alpha1`。另有 `test/e2e/framework/karmadactl.go:148` 的 timeout 错误拼接路径触发 race detector 报告。当前判断：更像 e2e/control-plane flake 或 CI 资源压力下的 aggregated API 短暂不可用，不是 workflow runner label 机械替换引入的确定性代码问题；建议先 rerun failed job，若无权限再经用户确认推 signed-off 空提交重触发 CI。
- 2026-07-08：尝试直接重跑 PR #7728 失败 job：`gh run rerun --failed` 和 `gh run rerun --job 85774432012` 均被 GitHub 拒绝；REST API 返回 `403 Must have admin rights to Repository`。当前账号不能直接重跑 upstream job。替代方案：请维护者 rerun，或经用户确认后向 `origin/chore/update-github-runner-ubuntu-24` 推 signed-off 空提交触发新一轮 PR CI。
- 2026-07-08：用户确认后，已在 `/tmp/karmada-ci-ubuntu24` 的 `chore/update-github-runner-ubuntu-24` 分支创建并推送 signed-off 空提交 `de3b6be675 test: trigger ci` 到 `origin/chore/update-github-runner-ubuntu-24`，用于重触发 PR #7728 CI。PR head 已更新为 `de3b6be675bbf8ad12f91052f7d0fb53c5b592a5`；upstream PR checks 已 queued/in_progress。fork push CI 同步触发：`CI Workflow` run `28915975049`、`Chart` `28915975027`、`CLI` `28915975040`、`Operator` `28915975026`、`FOSSA` `28915975012`，`image-scanning` `28915975025` skipped。下一步重点观察 upstream `CI Workflow / e2e test (v1.35.0)` 是否复现。
- 2026-07-08：核实原实现提交 `0f62fd62b05802961447601da9000403139b600d` 的 fork push CI 已全完成：`CI Workflow` passed，包含 lint/codegen/compile/unit/e2e v1.34/v1.35/v1.36；`Chart`、`CLI`、`Operator` 的 Kubernetes v1.34/v1.35/v1.36 矩阵均 passed；`FOSSA` 和 `image-scanning` skipped。已在 `internship-reports/day10-ci-ubuntu-runner-upgrade.md` 追加更新后的 PR body 草稿，用于把当前 PR body 里 “e2e jobs are still running” 改为最终 fork CI 结果，并说明 `de3b6be675` 是空提交重触发 CI。
- 2026-07-08：用户确认后已更新 upstream PR #7728 body。`gh pr edit` 因 token 缺少 `read:org` scope 被 GraphQL 拒绝；随后用 REST API `PATCH /repos/karmada-io/karmada/pulls/7728` 成功更新。PR body 现在包含 `0f62fd62b` 的完整 fork push CI passed 结果，并说明 `de3b6be675` 是 signed-off empty commit，只用于重触发 PR CI。
- 2026-07-08：PR #7728 收到 `@RainbowMango` 维护者评论，感谢 runner 更新并邀请继续处理 `karmada-io/dashborad` 的 Ubuntu 22 workflow。核对后确认 `dashborad` 是拼写错误，实际仓库是 `karmada-io/dashboard`，默认分支 `main`；其中 6 个 workflow 文件仍有 18 处 `runs-on: ubuntu-22.04`。当前没有相关 open PR/issue，且 `ranxi2001/dashboard` fork 不存在。建议单独做 dashboard PR，不混入 #7728；如继续，先 fork dashboard、机械替换为 `ubuntu-24.04`、跑 fork push CI，再开 upstream PR。可回复维护者：`Thanks for pointing this out. I checked that the remaining ubuntu-22.04 usages are in karmada-io/dashboard; I will handle them in a separate dashboard PR.`
- 2026-07-08：按用户要求把 dashboard 和 website 分开准备 PR，并先 fork。已创建 `ranxi2001/dashboard` 和 `ranxi2001/website` fork。dashboard worktree `/tmp/karmada-dashboard-ubuntu24`，branch `chore/update-github-runner-ubuntu-24`，commit `8f6ba046914e3e1bcc3a4d94f33912c10e33c64f`，6 个 workflow 文件 18 处 `ubuntu-22.04` -> `ubuntu-24.04`，本地 grep/diff-check/YAML parse 通过，fork push CI `CI Workflow` run `28917428886` passed，包含 lint/unit/build/e2e-frontend v1.33/v1.34/v1.35。website worktree `/tmp/karmada-website-ubuntu24`，branch `chore/update-github-runner-ubuntu-24`，commit `24e7dd4515225a9462b47e04a7ca79285a586964`，2 个 workflow 文件 2 处替换，本地 grep/diff-check/YAML parse/CRLF 检查通过，fork push CI `Typos Check` run `28917428642` passed；`CRLF Check` 仅 pull_request 触发。后续 PR 创建结果和最终验证现统一归档在 Day 10。
- 2026-07-08：用户确认后已创建两个 follow-up PR：`karmada-io/dashboard#643` 和 `karmada-io/website#1036`，标题均为 `ci: update GitHub runners to Ubuntu 24.04`。Dashboard PR DCO passed，upstream `CI Workflow` 和 `CodeQL` 正在运行；Website PR DCO passed，`CRLF Check` passed，`Typos Check` 和 Netlify checks 正在运行。主仓库 `karmada-io/karmada#7728` 新一轮 PR CI 已全绿：lint/codegen/compile/unit/e2e v1.34/v1.35/v1.36、Chart/CLI/Operator 全部 passed，DCO passed。尚未回复 `@RainbowMango` 评论；可回复草稿见 Day 10。
- 2026-07-06：按用户反馈修正 upstream issue #7719 正文格式：把 validation branch、commit、fork CI run 从代码块改为 Markdown 超链接列表；同时同步更新 `internship-reports/day9-community-issue-pr-ci-watch.md` 中的 issue 草稿和 PR 草稿验证段，避免后续 PR body 继续使用代码块链接。
- 2026-07-06：按用户确认发布 estimator / FlinkDeployment / ResourceQuota e2e flake upstream issue：[karmada-io/karmada#7719](https://github.com/karmada-io/karmada/issues/7719)，标题 `FlinkDeployment e2e cleanup can leave stale APIEnablements`，正文使用 Day 9 英文草稿，包含 #7697 失败 CI、scheduler missing API 日志、本地 diagnostic、候选修复 commit `f2e7c434b` 和 fork CI `28774018375` 全绿证据。第一次尝试 `gh issue create --label kind/flake` 因权限未加上 label，后续已按 Karmada/Prow 项目格式在 issue 下评论 `/kind flake`，当前 `kind/flake` label 已生效。Day 9 的 PR 草稿已改为 `Fixes #7719`。
- 2026-07-06：按用户要求先拟定 estimator / FlinkDeployment / ResourceQuota e2e flake 的 upstream issue。已读取 Karmada `flaking-test` issue 模板，检索 open/history issue 未发现直接重复项；在 `internship-reports/day9-community-issue-pr-ci-watch.md` 中新增“独立 issue 草稿”，标题为 `FlinkDeployment e2e cleanup can leave stale APIEnablements`，正文包含 #7697 失败 job、scheduler missing API 日志、本地 diagnostic 复现旧 cleanup 边界、与 #7692/#7551/#7550 的关系、以及验证分支 `test/estimator-flink-crd-flake` commit `f2e7c434b` / fork CI `28774018375` 全绿证据。未发布 upstream issue。
- 2026-07-06：完成 estimator / FlinkDeployment / ResourceQuota e2e flake 原型验证、分类和本地 diagnostic 复现。原始 #7697 失败应归类为 e2e timing flake 现象：同一业务代码 rerun 后通过，且失败路径不在 #7697 diff 内。独立 worktree `/tmp/karmada-estimator-flake` 分支 `test/estimator-flink-crd-flake` 第一版提交 `c4e514e171` 把 `WaitCRDDisappearedOnClusters` 放在 `ClusterPropagationPolicy` cleanup 后，fork CI run `28766135233` 失败：e2e v1.34/v1.35/v1.36 都在 `ScheduleMultiTemplate / FlinkDeployment scheduling` cleanup 等 member CRD 消失超时。修正版 `78d99e024` 只等 control/member CRD 消失，fork CI run `28767618548` 全绿。本地 diagnostic 使用 `/tmp/karmada-cert-rotate-multinode-observe/data/karmada/rotated-karmada.config` 复现旧 cleanup 边界：control plane CRD 1s 后消失，但 after 0s member CRD/APIEnablements 仍存在，after 2-3s member CRD 已消失而 APIEnablements 仍显示 FlinkDeployment，after 4s 才完全收敛。基于该证据，当前候选提交升级为 `f2e7c434bad6d4a970265af79a157afb61e6182e`：新增 `WaitCRDDisappearedFromClusterStatus`，三个 FlinkDeployment CRD cleanup 同时等待 control CRD、member CRD object、`Cluster.Status.APIEnablements` 收敛；本地 `git diff --check` 和 `go test ./test/e2e/suites/base -run '^$' -count=0` 通过，fork CI run `28774018375` 全绿（Chart/CLI/Operator、lint/codegen/compile/unit、e2e v1.34/v1.35/v1.36）。未开 upstream PR，未发 upstream 评论。
- 2026-07-06：用户指出 #7697 同一提交 rerun 通过说明 CI/e2e 确实存在偶发性失败问题，后续可独立跟进。已在 Day 9 追加“后续可跟进的 e2e 稳定性问题”，并在 TODO 新增 P1 任务：围绕 estimator / FlinkDeployment / ResourceQuota timeout 先检索已有 flake issue，再决定补证据、开独立 issue 或尝试最小 e2e wait 修复。注意不要把这个 flake 修复混进 #7697 当前证书轮换 PR。
- 2026-07-06：澄清 #7692 与 #7697 失败路径的关系：#7692 确实是在做 e2e flake 修复，但覆盖的是 `test/e2e/suites/base/clusterresourcebinding_test.go` 里 cleanup 前未等 member cluster `ClusterRole` present 的问题；#7697 这次失败是在 `test/e2e/suites/base/estimator_test.go` 的 FlinkDeployment / ResourceQuota / estimator assumption 流程。#7692 可作为同类异步等待修复模板，不等于 estimator flake 已有修复 PR。
- 2026-07-06：确认 upstream PR #7697 空提交 `93eaf7e57515c959fe30fa2aba387ce10029046d` 的新一轮 CI 已跑完并全绿：DCO、lint、codegen、compile、unit test、e2e v1.34.0/v1.35.0/v1.36.1，以及 Chart/CLI/Operator 三套 Kubernetes 矩阵均通过。此前失败的 `e2e test (v1.34.0)` 没有复现，当前证据支持将原 FlinkDeployment / ResourceQuota timeout 归类为 e2e timing flake，不需要为它修改 #7697 代码。剩余门禁是 `tide` pending：缺 `approved` / `lgtm` 标签，需要 maintainer review；Codecov patch coverage warning 是非阻塞提示。最终结果已补入 `internship-reports/day9-community-issue-pr-ci-watch.md`。
- 2026-07-06：按用户要求深读 #7692 和 #7663，结论已补入 `internship-reports/day9-community-issue-pr-ci-watch.md`。#7692 是最小 e2e flake 修复：`creates work with permanent ID label` 原来只等 control plane `Work` 创建，cleanup 却等待 member cluster `ClusterRole` 消失；PR 增加 `WaitClusterRolePresentOnClustersFitWith`，确保资源经历 present -> cleanup -> disappear。#7663 是运行态 bearer token reload：移除 `rest.Config.BearerToken` 静态 token，新增 `tokenRefreshingRoundTripper` 从 `Cluster.Spec.SecretRef` Secret 按 30s TTL 重新读 token，在请求前注入 Authorization；它只覆盖 push-mode member cluster bearer token，不覆盖 X.509 client/server cert、CAData、pull-mode agent 或无需重连的既有 watch。已在临时 worktree `/tmp/karmada-pr7663` 跑 `go test ./pkg/util -run 'Test(TokenRefreshingRoundTripper|BuildClusterConfig_LongLivedClientPicksUpRotatedToken)' -count=1`，结果通过。这个深读支撑 #7697 的能力边界：rotate 更新凭据数据，不等于运行中组件自动加载新凭据；运行态 reload 需要像 #7663 一样在具体 client/transport 路径单独实现和测试。
- 2026-07-06：按用户要求开始 Day 9 社区 issue / PR 扫描，同时等待 #7697 空提交触发的新 CI 完成。新增 `internship-reports/day9-community-issue-pr-ci-watch.md` 并加入 README/TODO。扫描结论：近期看似可做的 issue 多数已有认领或对应 PR，例如 #7691 -> #7692、#7688 -> #7689、#7670 -> #7671、#7695 -> #7696、#7698 -> #7699；不建议重复实现。后续最值得读的是 #7663 token rotation hot reload PR，用来补充 #7697 “轮换凭据数据”和“运行中组件加载新凭据”的能力边界；#7692 可作为 e2e timing flake 修复模式参考。#7697 最新快照：DCO/lint/codegen/compile/unit 已通过，Chart/CLI/Operator v1.34/v1.35/v1.36 已通过，仅三个 e2e job 仍 pending。
- 2026-07-06：用户确认采用空提交方案触发 PR #7697 新一轮 CI。已在 `/home/karmada` 的 `feature/cert-mode-rotate` worktree 上创建 signed-off 空提交 `93eaf7e57515 test: trigger ci`，并推送到 `origin/feature/cert-mode-rotate`。PR #7697 head 已更新为 `93eaf7e57515c959fe30fa2aba387ce10029046d`，upstream pull_request CI 已开始：CI Workflow `28762872757`、Chart `28762872753`、CLI `28762872770`、Operator `28762872747`；fork push CI 也已开始：CI Workflow `28762870959`、Chart `28762870940`、CLI `28762870936`、Operator `28762870928`。DCO 已通过。
- 2026-07-06：排查 upstream PR #7697 当前唯一失败 check：`e2e test (v1.34.0)`，run `28499042349` / job `84472927003` / head SHA `152ab454265ac683f55f04a166e9de9aedaad94c`。失败用例是 `test/e2e/suites/base/estimator_test.go` 的 `[EstimatorAssumption] ResourceQuota plugin assumption testing`，等待 FlinkDeployment 对应 `ResourceBinding` 变为 `Scheduled=True` 超时；scheduler 日志显示当时认为 `member1` 缺少 `flink.apache.org/v1beta1/FlinkDeployment` API。#7697 改动只在 `pkg/karmadactl/cmdinit` 和生成的 CLI flags doc，且 compile/lint/unit/codegen 与 e2e v1.35/v1.36 都通过，初步分类为 e2e timing flake / cluster API discovery 同步时序问题，建议优先 rerun 失败 job，而不是修改 #7697 代码。细节已记录到 `internship-reports/day8-after-pr7697-follow-up-pr-split.md`。
- 2026-07-01：用户确认后，已将 #7697 follow-up PR comment 发布到 `karmada-io/karmada#7697`：https://github.com/karmada-io/karmada/pull/7697#issuecomment-4853917542 。评论说明设计理念、运行态多节点 kind 验证结果，以及 rotate 与组件加载新证书之间的能力边界。已用 `thread_brief.py 7697` 和 `gh pr view --comments` 确认 comment 可见；Day 8 报告已补充发布链接。
- 2026-07-01：按用户要求继续整理 #7697 follow-up PR comment 草稿，已追加到 `internship-reports/day8-after-pr7697-follow-up-pr-split.md` 末尾。草稿为英文，可直接复制到 PR，内容覆盖设计理念、运行态多节点 kind 验证结果、以及“rotate 更新证书数据”和“重启/自动加载新证书恢复工作”的能力边界；明确 #7697 只实现 init-managed leaf certificate renewal + Secret/kubeconfig update，不承诺自动过期检测、自动 rotate、自动 rollout、热加载保证、CA rotation 或 caBundle migration。未发布 upstream comment，发布前仍需用户确认完整文本。
- 2026-07-01：按用户要求在 `internship-reports/day8-after-pr7697-follow-up-pr-split.md` 末尾追加“为什么 rotate 后还需要重启相关组件”。说明 Secret 更新与组件进程加载新证书是两个阶段；Karmada init-managed 组件通过 Secret volume / kubeconfig 文件获取证书，但 #7697 不定义统一热加载、watcher 或自动 rollout 机制；若证书已过期，必须先 rotate 更新 leaf cert Secrets，再重启 etcd/apiserver/controller-manager/scheduler/webhook 等相关组件读取新证书，单纯重启旧过期 Secret 不会恢复。
- 2026-07-01：更新实习阶段目标：在 2026 年 9 月前拿到 AgentCube Karmada 项目社区席位。目标拆解为：至少持续维护 1 条高质量 upstream feature 主线，形成可复查 issue/PR/CI/review 证据；参与 Karmada issue/PR 讨论和会议表达；把每次社区反馈转化为测试、文档或代码改进；避免只做本地学习而缺少 upstream 可见产出。
- 2026-07-01：严格按 Day 6 文件级变更清单从 `upstream/master` 新建 upstream-facing 分支 `feature/cert-mode-rotate`，实现 `karmadactl init --cert-mode=rotate` 第一版。提交 `32e553925 feat: support rotating init managed certificates` 已推送到 `origin/feature/cert-mode-rotate` 触发 fork push CI。实现范围保持在 `pkg/karmadactl/cmdinit` 和生成的 `docs/command-line-flags/karmadactl_init.md`：新增 cert mode flag/config 字段、拆分 `Complete()` / `RunInit()`、新增 rotate 独立路径读取既有 CA 并重新签发 leaf cert、update-only 同步既有 cert Secrets；未修改 `command.go`、`deployments.go`、`statefulset.go`、Helm/operator、caBundle 或 workload 模板。本地验证已通过：`go test ./pkg/karmadactl/cmdinit/... -count=1`、`go test ./pkg/karmadactl/... ./cmd/karmadactl/... ./cmd/kubectl-karmada/... -count=1`、`hack/verify-command-line-flags.sh`、`hack/verify-import-aliases.sh`、`PATH="$(go env GOPATH)/bin:$PATH" golangci-lint run ./pkg/karmadactl/cmdinit/...`、`PATH="$(go env GOPATH)/bin:$PATH" hack/verify-staticcheck.sh`、`git diff --check`。
- 2026-07-01：按用户要求把“先架构/文件级 scope 设计，再写代码”的开发模式沉淀为本地 skill `.agents/skills/design-before-code/`。该 skill 要求在非平凡代码任务前先读现有实现、产出 file scope matrix、no-change list、Mermaid current/proposed flow、函数级设计和测试矩阵；如果实现中发现 scope 扩大，必须先更新设计再继续。已通过 `quick_validate.py`。
- 2026-07-01：按用户要求继续补充 `internship-reports/day6-certificate-rotation-design-implementation.md`，在末尾新增“代码级别文件变动设计”。内容包括文件触点总览、建议变更清单、第一版明确不改的文件、`RunInit()` / `Complete()` 拆分、CA 与 leaf cert 边界、Secret 更新边界、函数级草案、测试矩阵、实现顺序和 PR body 解释。当前设计结论保持不变：第一版聚焦 `karmadactl init --cert-mode=rotate`，只更新由 init 管理的证书 Secrets，复用既有 CA，不轮转 root CA，不更新 caBundle，不改 workload 模板。
- 2026-07-01：按用户要求开始准备社区会议提案。新增 `internship-reports/day7-certificate-rotation-community-proposal.md`，把 #7693 方案整理成可上会讲的 one-pager、英文 30 秒/3 分钟 talk track、scope/non-goals、Mermaid current/proposed flow、resource touch matrix、测试计划、PR 拆分和 maintainer open questions。核心会议主张：第一版聚焦 `karmadactl init --cert-mode=rotate`，只轮转组件身份证书/leaf certificates，复用既有 CA，不轮转 root CA、不更新 caBundle、不重建 workload；会议需要确认 CLI UX、CA private key 来源、是否只打印重启指导、external etcd 缺省行为。
- 2026-06-30：按用户要求新增 `internship-reports/day6-certificate-rotation-design-implementation.md`，整理 `karmadactl init` 证书轮换方案设计与实现准备。已对齐 [website#1014](https://github.com/karmada-io/website/issues/1014)、[karmada#4787](https://github.com/karmada-io/karmada/issues/4787)、[karmada#5037](https://github.com/karmada-io/karmada/pull/5037)、[website#1016](https://github.com/karmada-io/website/pull/1016) 背景。关键实现判断：#7693 第一版应聚焦 `karmadactl init --cert-mode=rotate`，复用证书生成和 Secret 更新路径；`Complete()` 当前含安装期 NodePort 检查、Node mutation、data path 清理，rotate mode 不能原样复用；根据用户确认，第一版轮转的是组件身份证书/leaf certificates，CA/root certificates 是底层信任链基石，不轮转、不更新 caBundle。
- 2026-06-30：跟进维护者新建的 upstream issue [#7693](https://github.com/karmada-io/karmada/issues/7693)。确认此前 Day 4 / #7690 / `feature/cert-manager-layout` 的重心偏向 split Secret layout 和长期证书管理抽象，而 #7693 给出的当前落地方向是先复用安装工具已有证书生成和 Secret 写入能力，为 `karmadactl init` 增加证书轮换模式，例如 `--cert-mode=rotate`。已更新 `internship-reports/day4-certificate-layout-issue-follow-up.md` 的剩余部分，补充“理解与对齐”：rotate mode 应复用 `genCerts()`、证书文件读取、`createCertsSecrets()` / `CreateOrUpdateSecret()`，但不继续执行 workload/CRD 创建；旧 split layout prototype 暂时只作为 Secret 映射参考。同步更新 TODO，把证书主线切到 #7693。
- 2026-06-30：按用户要求为 #7643 创建可复查证据分支。使用 `/home/karmada-issue-7643` 从 `upstream/master@ffbade988` 切到 `verify/issue-7643-evidence`，新增两个 evidence tests：`pkg/resourceinterpreter/customized/declarative/luavm/issue7643_verification_test.go` 和 `pkg/resourceinterpreter/default/thirdparty/issue7643_verification_test.go`。测试不仅打印输出，也断言 `0.1 -> 100m`、默认 Flink components 保持 `"100m"`、`CalculateResourceUsage` 汇总 `"200m"`。已运行两条目标 `go test` 均通过，并提交/推送 fork 分支：`ranxi2001/karmada@verify/issue-7643-evidence`，commit `46d0ba9be test: add flink memory conversion evidence`。同步更新 Day 5，把证据分支和 commit 链接加入 upstream 评论草稿。
- 2026-06-30：按用户要求把 #7643 可直接复制的 upstream 评论草稿追加到 `internship-reports/day5-issue-7643-flink-memory-verification.md`，包含 Mermaid 对比图、函数级验证输出、默认 FlinkDeployment customization 路径输出和当前结论。同步更新 `AGENTS.md`：以后准备 upstream comment、PR 描述、review 文案、Mermaid 解释等可复用长文本时，必须写入报告或草稿文件，不要只在终端/chat 输出里交付。
- 2026-06-30：跟进 upstream issue #7643，按 maintainer 要求做函数级和运行路径验证。使用独立 worktree `/home/karmada-issue-7643` 基于 `upstream/master@ffbade988` 添加临时测试，确认 `kube.getResourceQuantity("100m")` 返回 Lua number `0.1`，但 Lua -> Go JSON 转换后是 `resource.Quantity("100m")`；默认 FlinkDeployment thirdparty interpreter 输出 JM/TM memory 均为 `"100m"`，`helper.CalculateResourceUsage()` 汇总为 `{"cpu":"300m","memory":"200m"}`。结论写入 `internship-reports/day5-issue-7643-flink-memory-verification.md`：当前 upstream master 未复现 issue 描述的 “100m 变成 1 并导致总量错误”；若要回复 upstream issue，先让用户确认英文评论文本。
- 2026-06-30：按用户要求阅读官方 [Certificate Framework](https://karmada.io/docs/administrator/security/cert-framework/) 并对照仓库内 `docs/proposals/cert/Self-Signed_Certificate_Content_Standardization.md`、`hack/deploy-karmada.sh`、`artifacts/deploy/*.yaml`。更新 Day 4：补充官方 cert framework 证书定义词表和与 prototype 的差异。关键修正：当前官方框架已在 `hack/deploy-karmada.sh` 落地，`karmadactl init`/operator/Helm 未来对齐；prototype 还没有完全对齐官方 CA 模型和组件覆盖范围，尤其是保留 `EtcdCA`、未覆盖 search/metrics-adapter/scheduler-estimator server cert/search etcd client 等，需要 PR 前重新注释和确认。
- 2026-06-30：用户已在 #7690 下评论 `/kind feature`，issue label 已生效为 `kind/feature`。新增 Day 4 记录 `internship-reports/day4-certificate-layout-issue-follow-up.md`，分析 `feature/cert-manager-layout` branch 与 #7690 提案之间的 gap：branch 覆盖第一版 `karmadactl init` plan-based split layout prototype，但不覆盖图片中的长期证书管理系统；主要待确认点是 maintainer 方向、是否拆小 PR、命名规范、RBAC/client identity 是否 follow-up、以及真实 split layout smoke test。
- 2026-06-30：按用户确认发布 upstream issue。使用 Day 3 中已确认的英文草稿新建 `karmada-io/karmada#7690`：`Proposal: plan-based split certificate Secret layout for karmadactl init`，正文包含 #6051/#6670/#6788 关联、scope/non-goals、两张设计图 raw 链接、prototype branch/commit/CI 证据，并 mention `@zhzhuang-zju` 请求 review。已用 `thread_brief.py 7690` 确认为 open，暂无 labels、assignees、milestone、comments。
- 2026-06-30：按“新建 issue 最好带图片引用解释”的要求，增强 Day 3 upstream issue 草稿：加入两张已推到 fork `intern` 分支的设计图 raw 链接，并验证链接返回 `200 image/png`。草稿中明确图片只作为长期方向和前后对比说明，当前 issue/PR scope 仍限于 `karmadactl init` 的 plan-based certificate Secret layout，不引入 CRD/controller/cert-manager 集成。
- 2026-06-30：按“先做成 issue、让 @zhzhuang-zju review 提案”的社区流程要求，重新核对 #6051、#6670、#6788：#6051 是证书/配置命名规范 umbrella，#6670 是证书标准化 proposal，#6788 是已有 open split secret layout PR。已在 Day 3 增加英文 upstream issue 草稿，明确该提案与三者的关系、plan-based 抽象边界、prototype branch、CI 证据和待 maintainer 确认的问题；未发布 upstream issue/comment，也未自动 mention maintainer，等待用户确认目标和完整文本。
- 2026-06-30：确认 `feature/cert-manager-layout` commit `eb02bde96` fork push CI 全绿：18 个 check runs 中 16 success、2 skipped、0 failed。新增 Day 3 的 PR 审阅准备部分，整理当前 PR 的实现边界、未实现内容、文件修改解释、reviewer 重点关注点、本地验证命令、fork CI 结果和 upstream PR 文案草稿；补充两张方案图 `day3-karmada-certificate-management-proposal-data-flow.png`、`day3-karmada-certificate-management-comparison-data-flow.png` 作为长期方案/对比说明资料。
- 2026-06-30：分析 fork push CI 结果。`feature/cert-manager-layout` commit `651cbec29` 的 push CI 已全部结束，只有 `CI Workflow / lint` 失败；compile、unit test、codegen、3 个 e2e、CLI / Chart / Operator Kubernetes 矩阵均通过或 skipped。失败根因是新增 `pkg/karmadactl/cmdinit/certmanager` 包未满足 `.golangci.yml` 静态检查：导出符号缺 Go doc、Secret 名称常量触发 gosec G101 误报、测试循环可改 `slices.Contains`、`legacyCertificateNames` 参数未使用。已把复盘和提交前 lint checklist 写入 Day 3，并在 TODO 加入 P0 lint 习惯项。
- 2026-06-29：完成 Day 3 证书管理方向任务整理。新增 `internship-reports/day3-certificate-management-task-triage.md`，梳理 community#69、#6091、#6269、#6670、#6788、#6553 和 #6051 的关系；结论是优先跟进 #6051 中 Helm 证书 Secret / volume / mount path naming convention 的 `help wanted` 空位。随后按“不要为了改代码而改代码，先抽象证书管理层”的反馈，追加了批量系统证书替换分发的初步设计，包括证书身份、layout plan、Secret plan、组件 kubeconfig 分发和验证计划，并加入 Mermaid 前后对比图说明抽象层引入后的变化。同步更新 `internship-reports/README.md` 和 `internship-reports/todo.md`。
- 2026-06-29：按用户反馈修整 `internship-reports/day2-karmada-architecture.drawio`。改为真正用 draw.io CLI 导出并视觉检查，入口流程调整为 User -> Karmada API Server，Admission Webhook 画成 API Server 的 admission 调用；移除长边标签，用编号 1-6 + 底部图例表达流程；重新导出 `day2-karmada-architecture.png`、`day2-karmada-architecture.drawio.png`、`day2-karmada-architecture.drawio.svg`，`validate.py` 结果为 `0 error(s), 0 warning(s)`。当前 Day 2 相关文件已暂存，尚未 commit/push，等待用户确认图。
- 2026-06-29：完成 Day 2 Karmada 项目理解。新增 `internship-reports/day2-karmada-project-understanding.md`，面向首次接触 Karmada、仅有基础 K8s 概念的读者，用 Mermaid 梳理控制面、ResourceTemplate -> PropagationPolicy -> ResourceBinding -> Work -> member cluster 传播链路、nginx 样例和源码目录地图；新增 `internship-reports/day2-karmada-architecture.drawio` 作为可编辑架构图源文件，并导出 `internship-reports/day2-karmada-architecture.png`、`internship-reports/day2-karmada-architecture.drawio.png`、`internship-reports/day2-karmada-architecture.drawio.svg`。
- 2026-06-28：处理 GitHub fork sync 提示。`origin/master` 曾比 `upstream/master` 多 1 个 personal fork PR #1 merge commit `410202123`，同时落后 upstream 3 个 commit；已按 fork `master` 镜像规则把本地/远端 `master` force-with-lease 重置到 `upstream/master` `56d5d87ec`。当前 `origin/master` 与 `upstream/master` 一致，`intern` 保留实习记录不受影响。
- 2026-06-28：按 mentor 反馈同步修正 Karmada CI 验证规则：以后不再向个人 fork 仓库提 PR 来跑 CI。Karmada 与 AgentCube 不同，`.github/workflows/ci.yml` 已经明确支持 `push` 到 fork 分支触发 CI（排除 `dependabot/**`），所以预提交 upstream 前直接 push topic/validation branch 到 `origin` 并查看 commit SHA Actions/checks；如失败先分类为代码问题、fork 环境差异、缺少 tag/history、CI flake 或 upstream-only gate。已更新 `AGENTS.md` 和 `.agents/skills/karmada-pr-management/SKILL.md`。
- 2026-06-26：从 `master` 新建并切换到本地 `intern` 分支。
- 2026-06-26：参考 AgentCube 实习记录结构，开始为 Karmada 建立基础文件：根目录 `AGENTS.md`、`PROGRESS.md`、`internship-reports/README.md`、`internship-reports/todo.md`、`internship-reports/intern-glossary.md`，以及本地可复用 skill `.agents/skills/open-source-onboarding/`。
- 2026-06-26：从 AgentCube 迁移 3 个本地 skills 到 Karmada：`.agents/skills/drawio-skill/` 保留通用绘图能力；`agentcube-pr-management` 改造为 `.agents/skills/karmada-pr-management/`，默认 repo 为 `karmada-io/karmada`、基线分支为 `upstream/master`、测试命令按 Karmada `make test` / `make verify` / `hack/update-*`；`agentcube-issue-discussion` 改造为 `.agents/skills/karmada-issue-discussion/`，脚本默认 repo 改为 `karmada-io/karmada`。4 个本地 skills 已通过 `quick_validate.py`。
- 2026-06-26：配置 `upstream=https://github.com/karmada-io/karmada.git`，从 `upstream/master` 创建 topic branch `test/update-default-control-plane-images`，完成 #7598 follow-up：同步 Helm chart、`karmadactl init`、`karmada-operator`、raw deploy manifest 和 `hack/deploy-karmada.sh` 的默认 Kubernetes / etcd 版本，并提交 upstream PR #7666。

## Current Blockers

- 尚未实际运行 `hack/local-up-karmada.sh`，因此本机 kind / Docker / kubeconfig / 多集群环境是否可用还没有验证。
- 尚未跑完整 `make test` 或 `make verify`。本次 PR 只跑了相关范围的 `go test`、`hack/verify-command-line-flags.sh`、Helm lint、脚本语法和 YAML 解析。
- upstream PR #7666 的 GitHub Actions 仍需继续观察。
- GitHub CLI 当前未登录，匿名 GitHub API broad search 已遇到 rate limit；后续继续做 issue/PR 批量检索时应配置 `GH_TOKEN` 或改用浏览器页面。
- 当前 Windows 电脑上 draw.io 已安装但未加入 PATH：`drawio` / `draw.io` 命令不可用，系统级路径 `C:\Program Files\draw.io\draw.io.exe` 也不存在；实际可用路径是 `C:\Users\ranxi\AppData\Local\Programs\draw.io\draw.io.exe`，版本 `30.2.6`。这个路径只适用于当前 Windows 机器；macOS 或其他机器仍按 drawio-skill 的正常探测顺序处理。

## Ruled Out

- 不改写根 `README.md` 作为实习导航，避免污染上游项目介绍。
- 不把实习报告、中文学习笔记、本地 benchmark 原始数据或本地 skills 放到 upstream-facing topic branch。

## Next

- 以“9 月前拿到 AgentCube Karmada 项目社区席位”为主目标管理后续工作：每周至少沉淀 1 个 upstream-visible 证据，包括 merged PR、reviewed PR、issue 验证、社区会议材料、maintainer feedback follow-up 或可复用测试/文档改进。
- #7697 不再需要等待 CI；下一步是等待 maintainer review、`/lgtm` 和 `/approve`。如果 reviewer 追问运行态 reload 或重启边界，结合 Day 9 中 #7663 token refresh 深读和 Day 8 runtime validation 解释：#7697 更新 init-managed certificate data，组件加载新证书依赖重启或后续单独实现的 reload 机制。
- estimator / FlinkDeployment / ResourceQuota e2e flake 已发布 upstream issue #7719，`kind/flake` label 已生效；修复 PR [#7732](https://github.com/karmada-io/karmada/pull/7732) 已创建，commit `1240559dd` 本地验证和 fork push CI 均通过。下一步观察 upstream PR CI 与 review；如失败先按 Day 11 flake 口径区分代码问题和 CI flake，不要把这个修复混入 #7697。
- CI flake 专项下一步：补最近两次 `CI Schedule Workflow` 和 `APIServer compatibility` artifacts，把 37 个 schedule e2e/setup 失败按具体 Ginkgo spec 或 setup 阶段聚类；Remedy 若再次复现，准备引用 #5323 的英文 issue/comment 草稿并先给用户确认。
- #7693 后续 PR 前需要准备英文 PR body 和 reviewer 说明，明确第一版 scope：只轮转组件 leaf certificates，复用既有 CA，update-only 同步 init-managed Secrets，不轮转 root CA、不更新 caBundle、不自动重启、不改 Helm/operator/workload 模板。开 upstream PR 前必须让用户确认完整英文文本和目标。
- #7690 / `feature/cert-manager-layout` 暂时作为背景和 Secret 映射参考，不作为当前第一优先级 PR。旧 branch 已通过 fork push CI，但不要直接从它开 upstream PR。
- 如需跟进 #7643 upstream，先发送 Day 5 中的英文 verification comment draft 给用户确认；当前不建议开重复 PR，因为 issue 已有 assignee，且验证结果不支持 functional bug 结论。
- 继续观察 upstream PR #7666 的 CI 和 review；如果失败，先区分代码问题、环境抖动和 CI 环境差异。
- 阅读 Karmada 官方入口：`README.md`、`CONTRIBUTING.md`、`docs/README.md`、`docs/images/architecture.png`、`samples/nginx/README.md`。
- 补 Quick Start 预检报告：跑通或拆解 `hack/local-up-karmada.sh`，记录环境、失败命令、错误、绕过方式和最终结果。
- Day 3 深追 `samples/nginx` 传播链路：从 `Deployment` + `PropagationPolicy` 到 `ResourceBinding`、`Work` 和 member cluster manifest，重点读 `pkg/detector/`、`pkg/controllers/binding/`、`pkg/controllers/execution/`。
- 证书方向下一步：先把证书管理层设计收敛成更小的代码边界，避免在部署代码里散落 `split` 判断；再根据 mentor 方向决定是继续 `karmadactl init` split layout，还是回到 #6051 Helm 证书 Secret / volume / mount path 差距表。
- 如果要找第一个贡献点，先从 docs、tests、good first issue、flaking test、CLI help 或小型 validation/test gap 入手。
- 需要绘制 Karmada 架构图、传播链路图或调度流程图时，使用 `.agents/skills/drawio-skill/`。
- 分析 Karmada issue/PR 或准备 upstream PR 时，优先使用 `.agents/skills/karmada-issue-discussion/` 和 `.agents/skills/karmada-pr-management/`。

## Stop Conditions

- 同一个本地环境问题连续失败 3 次，例如 kind 集群创建、Docker 镜像拉取、证书生成或 kubeconfig 上下文混乱，就停止硬调，记录 BLOCKED 并换路径。
- 如果某个 issue 已有人明确认领并正在修改同一问题，不开重复 PR，只做复现、测试、review 或文档补充。
- 如果只能基于猜测发社区评论，先停止，补源码证据、官方文档引用或本地验证结果后再讨论。
